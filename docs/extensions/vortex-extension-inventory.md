# Vortex Extension Inventory

Source: local clone `/tmp/dmm-vortex`
Remote: `ssh://git@github.com/Nexus-Mods/Vortex.git`
Source commit: `c57894eb71af8234b58a6bd15ae5ab543eccac3a`
Collected: 2026-08-08

This is the parity backlog for DMM extensions and shared extension capabilities. Each Vortex extension below gets a DMM counterpart: either a game extension, a shared extension-framework capability, a provider/store capability, a mod-type capability, or an explicit documented decision that the Vortex extension is not applicable to DMM.

Status legend:

- `[x]` means a DMM counterpart already exists and a duplicate should not be created.
- `[ ]` means no DMM counterpart has been identified yet.
- `Counterpart exists` does not mean full Vortex parity is complete; verify source parity before marking implementation work complete elsewhere.

Rules:

- Verify each Vortex extension from source before implementing parity behavior.
- Keep game-specific behavior inside the DMM game extension.
- Add generic extension-framework APIs when multiple games or mod types need the same kind of capability.
- Do not mark an entry complete only because a placeholder DMM extension exists.

Counts:

- Framework/shared Vortex extensions: 46
- Game Vortex extensions: 86
- Total Vortex extension entries: 132
- DMM counterparts identified in the current pass: 132
  - Framework/shared counterparts: 46
  - Game counterparts: 86

## Framework And Shared Extensions

- [x] `changelog-dashlet` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed dashlet/state metadata marked blocked until DMM has a generic extension UI/state runtime.
- [x] `common-interpreters` - DMM counterpart: `internal/extensions/commoninterpreters` framework extension. Current parity is registered interpreter metadata for `.jar`, `.py`, `.vbs`, `.cmd`, and `.bat`; runtime execution remains gated until a converted extension needs interpreter launching.
- [x] `documentation` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed main-page/action/todo metadata marked blocked until DMM has a generic extension UI runtime.
- [x] `extension-dashlet` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed dashlet metadata marked blocked until DMM has a generic extension UI runtime.
- [x] `feedback` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed feedback main-page/dialog/action/state metadata marked blocked; public feedback submission is not part of Steam Deck MVP.
- [x] `fnis-integration` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension. Current parity is source-backed FNIS settings/action/test/todo metadata and blocked `deploy-single-mod` dependency until DMM has an external-tool/deploy-single-mod runtime.
- [x] `gamebryo-archive-check` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework metadata plus extension-declared Gamebryo plugin activation diagnostics. Current parity is source-backed incompatible BSA/BA2 archive version detection for enabled managed plugins using the Vortex allowlists. BA2 write and non-Gamebryo archive engines remain separate blocked archive-type capabilities.
- [x] `gamebryo-archive-invalidation` - DMM counterpart: shared `internal/extensions/gamebryo` archive-invalidation handler.
- [x] `gamebryo-archive-support` - DMM counterpart: `internal/extensions/gamebryoarchive` framework extension plus `internal/gamebryoarchive` runtime. Current parity is source-backed native BA2/BSA list/read/extract support, including BA2 GNRL/DX10 name tables and BSA v103/v104/v105 extraction, plus native BSA create/write support for uncompressed v103/v104/v105 archives.
- [x] `gamebryo-bsa-support` - DMM counterpart: `internal/extensions/gamebryoarchive` framework extension plus `internal/gamebryoarchive` runtime. Current parity is source-backed native BSA list/read/extract support and native BSA create/write support for uncompressed v103/v104/v105 archives.
- [x] `gamebryo-plugin-indexlock` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension plus profile plugin-activation state. Current parity is source-backed locked-index state and generated load-order behavior; the exact Vortex plugin-table control remains blocked until DMM exposes a polished plugin table/detail UI.
- [x] `gamebryo-plugin-management` - DMM counterpart: shared `internal/extensions/gamebryo` plugin activation capability. Full Bethesda load-order/sorting parity remains tracked separately.
- [x] `gamebryo-savegame-management` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension. Current parity is source-backed savegame state/action/main-page/profile-feature metadata marked blocked until DMM implements savegame profile management.
- [x] `gamebryo-test-settings` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework metadata plus `internal/extensions/gamebryo` settings diagnostics. Current parity is source-backed Oblivion `Oblivion.ini` missing-font detection, Vortex-equivalent automatic Oblivion INI font repair through an extension-owned diagnostic repair hook, and Skyrim-family `fontconfig.txt` validation against `Skyrim - Interface.bsa`.
- [x] `gameinfo-steam` - DMM counterpart: core Steam library/app manifest discovery. This is core platform capability in DMM, not a separate game extension.
- [x] `gamestore-gog` - DMM counterpart: `internal/extensions/gamestores` framework extension. Current parity is source-backed metadata marked blocked because Vortex uses Windows GOG Galaxy registry/client integration.
- [x] `gamestore-origin` - DMM counterpart: `internal/extensions/gamestores` framework extension. Current parity is source-backed metadata marked blocked because Vortex uses Windows Origin/EA manifest and protocol integration.
- [x] `gamestore-uplay` - DMM counterpart: `internal/extensions/gamestores` framework extension. Current parity is source-backed metadata marked blocked because Vortex uses Windows Ubisoft registry/protocol integration.
- [x] `gamestore-xbox` - DMM counterpart: `internal/extensions/gamestores` framework extension. Current parity is source-backed metadata marked blocked because Vortex uses Windows Xbox app registry and shell launch integration.
- [x] `gameversion-hash` - DMM counterpart: `internal/extensions/gameversionhash` framework extension. Current parity covers source-backed `hashFiles`/`hashDirPath` providers for converted game extensions, Vortex-style chained MD5 calculation, and Vortex backend hash-map lookup with raw-hash fallback. The generic Vortex UI/API action for editing hash-map entries remains metadata-only.
- [x] `issue-tracker` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed issue dashlet/dialog/state metadata marked blocked until DMM has a generic issue/reporting runtime.
- [x] `local-gamesettings` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension. Current parity is source-backed profile-feature/test metadata and blocked `bake-settings` API until DMM implements profile-local game settings.
- [x] `meta-editor` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed metadata editor dialog/action/state metadata marked blocked until DMM has a generic metadata edit runtime.
- [x] `mo-import` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed MO import dialog/action metadata marked blocked; actual MO import remains a future migration feature.
- [x] `mod-content` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed mod-content table/action metadata marked blocked until DMM has generic extension table attributes/actions.
- [x] `mod-dependency-manager` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension plus the shared runtime metadata dependency evaluator. Current parity evaluates extension-extracted required/recommended metadata dependencies for enabled profile mods, including minimum versions and logical-name aliases. Duplicate managed file targets now block deployment until a profile file-winner is selected, except where an extension declares source-backed conflict-ignore metadata. Game extensions can decide whether a source-backed dependency is blocking; Stardew SMAPI manifest dependencies are intentionally reported as recommendations to match the verified Vortex Stardew behavior. Vortex rule editing, dependency install actions, broader conflict graph UI, cycle handling, and generic `unfulfilled-rules` extension overrides remain blocked.
- [x] `mod-highlight` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed mod highlight table/action/state metadata marked blocked until DMM has generic table attributes and UI state.
- [x] `mod-report` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed mod report action metadata marked blocked until DMM has a reporting/export flow.
- [x] `modtype-bepinex` - DMM counterpart: shared `internal/extensions/bepinex` Unity/BepInEx installer/runtime capability.
- [x] `modtype-dazip` - DMM counterpart: `internal/extensions/dazip` framework helper. Current parity covers Vortex `dazipOuter` nested `.dazip` extraction, `dazipInner` planning for extracted DAZIP contents, Dragon Age Origins AddIns.xml generation from enabled managed manifests, and historical DA2 purge migration through the typed purge state-migration runtime.
- [x] `modtype-dinput` - DMM counterpart: `internal/extensions/sharedmodtypes` framework extension. Current parity covers the source-backed DInput mod type and installer: archives containing `dinput8.dll` are rooted at the wrapper folder around that DLL, require an installer-choice trust confirmation before planning, and deploy beside the extension-declared game executable.
- [x] `modtype-enb` - DMM counterpart: `internal/extensions/sharedmodtypes` framework extension. Current parity is source-backed ENB mod type metadata marked blocked until a source-backed game-root runtime path exists; Vortex's ENB installer registration is currently commented out upstream.
- [x] `modtype-gedosato` - DMM counterpart: `internal/extensions/sharedmodtypes` framework extension. Current parity is source-backed GeDoSaTo all-texture archive matching, texture-folder target-root helper, and explicit `DMM_GEDOSATO_PATH`/`GEDOSATO_PATH` tool-location requirement for game extensions that opt in; generic extension-declared tool discovery now exists.
- [x] `modtype-umm` - DMM counterpart: shared `internal/extensions/umm` helper plus source-backed framework metadata. Current parity covers UMM game opt-in metadata, Vortex Mods-folder installer registration for converted UMM games, source-backed `umm-installer` tool archive staging through the reusable DMM `tool-only` deployment mode, extension-declared acquisition for Vortex's UMM 0.24.2 Nexus/GitHub package through the captured-install pipeline, Vortex browse instructions preserved in acquisition metadata, active-game auto-acquire triggers for `autoDownloadUMM`, generic discovery of staged/declared tool payloads, default-primary metadata for managed UMM tools, and Decky-owned launch requests for ready executable tools. Unity Mod Manager noninteractive patch execution remains blocked.
- [x] `morrowind-plugin-management` - DMM counterpart: `internal/extensions/morrowind` plus `internal/extensions/vortexsharedsystems` framework metadata. DMM implements the source-backed Morrowind plugin activation path through restore-aware `Morrowind.ini` Game Files generation and ESP/ESM timestamp ordering in the Morrowind game extension. The separate Vortex plugin-management drag/drop page, filesystem watcher state, and generic UI wrapper remain blocked.
- [x] `mtframework-arc-support` - DMM counterpart: `internal/extensions/mtframeworkarc` framework extension plus `internal/arctool` runtime. Current parity is source-backed ARC archive type metadata and a typed ARCtool process bridge for Vortex list/extract/create semantics. Dragon's Dogma now wires that bridge through an extension-owned deploy hook for Vortex's selective `game_main.arc`/`title.arc` merge behavior. Native ARC parsing/repacking remains pending; DMM uses ARCtool when `DMM_ARCTOOL_PATH` points at the helper executable.
- [x] `new-file-monitor` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension. Current parity is source-backed lifecycle/new-file-adoption metadata; DMM lifecycle events exist but managed adoption UI/runtime remains blocked.
- [x] `nmm-import-tool` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed NMM import dialog/action/todo/state metadata marked blocked; actual NMM import remains a future migration feature.
- [x] `open-directory` - DMM counterpart: typed `RegisterExtensionAction` open-directory targets plus Decky-owned execution. Current parity covers source-backed open-folder actions for extension-declared game, downloads, staging, and target-root paths with backend path resolution and Decky reporting.
- [x] `quickbms-support` - DMM counterpart: `internal/extensions/quickbmssupport` framework extension plus `internal/quickbms` runtime. Current parity is source-backed QuickBMS API metadata for register/list/extract/write/reimport and a typed process bridge covering Vortex's flags, wildcard filters, logging, timeout, and list parsing. Remaining work is wiring converted game extensions through a first-party DMM extension API namespace when a source-verified game actually needs QuickBMS.
- [x] `script-extender-error-check` - DMM counterpart: shared `internal/extensions/gamebryo` script-extender runtime requirement capability. Full Vortex parity still needs source review per game/tool.
- [x] `script-extender-installer` - DMM counterpart: shared `internal/extensions/gamebryo` script-extender installer capability.
- [x] `test-gameversion` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension. Current parity is source-backed game-version tests plus executable `registerGameInfoProvider` rows exposed through `/api/games/{appID}/info`, phone/tablet Review, and the selected-game Decky surface.
- [x] `test-setup` - DMM counterpart: `internal/extensions/vortexsharedsystems` framework extension. Current parity is source-backed setup test metadata marked blocked until DMM has generic setup/uninstall-entry test runtime.
- [x] `theme-switcher` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed theme settings/state metadata marked blocked; custom theming is not part of Steam Deck MVP.
- [x] `titlebar-launcher` - DMM counterpart: `internal/extensions/vortexuisurfaces` framework extension. Current parity is source-backed titlebar launcher settings/state metadata marked blocked; DMM launch controls are Decky-native.

## Game Extensions

Source-backed catalog coverage:

- `internal/extensions/vortexgamecatalog` is currently empty. It remains available only as a temporary source-reviewed metadata shim when a verified Vortex entry cannot yet be promoted to a dedicated first-party DMM extension.
- Catalog entries must not claim executable install parity. When the Vortex source registers custom installers, mod types, load order, lifecycle behavior, or other game-specific runtime logic that DMM has not ported yet, the DMM extension must expose blocked metadata so the UI/API can report the gap instead of silently installing unsafely.

- [x] `game-7daystodie` - DMM counterpart: `internal/extensions/sevendaystodie`. Source-backed parity covers Vortex `ModInfo.xml` modlets deployed into the configured User Data Folder `Mods` target root, root BepInEx-style packages, deterministic profile-priority folder prefixes, extension setting storage for the UDF path, setup metadata for ensuring the resolved Mods folder, and a generic setting-driven Steam launch-argument requirement for `-UserDataFolder="..."`. The Vortex prefix-offset dialog remains blocked until DMM has generic load-order action UI.
- [x] `game-ahatintime` - DMM counterpart: `internal/extensions/ahatintime`. Source-backed installer parity covers Vortex `modinfo.ini` archive handling into `HatInTimeGame/Mods` plus the Modding Tools supported-tool metadata.
- [x] `game-baldursgate3` - DMM counterpart: `internal/extensions/baldursgate3`. Source-backed parity covers Vortex local Mods and local app-data target roots adapted to Steam Deck Proton, BG3 pak/modfixer/BG3SE/engine-injector/replacer/LSLib installer routing, Divine tool/runtime acquisition through the shared acquisition contract, Public profile `modsettings.lsx` setup with Vortex's v8 default template, and Divine-backed `meta.lsx` extraction during extension-owned `modsettings.lsx` generation. BG3 load-order import/migration UI remains future parity work.
- [x] `game-battletech` - DMM counterpart: `internal/extensions/battletech`. Source-backed installer parity covers Vortex Documents `My Games/BattleTech/mods` deployment adapted to Steam Deck Proton Documents plus `BattleTech_Data/StreamingAssets/version.json` ProductVersion parsing. DMM now ports Vortex `added-files` adoption through reusable snapshot/diff runtime plus BattleTech's single-owner generated-file adoption handler.
- [x] `game-bladeandsorcery` - DMM counterpart: `internal/extensions/bladeandsorcery`. Source-backed installer parity covers Vortex official `manifest.json` mods, engine-injection routing through the shared `dinput` mod type, blocking obsolete MulleDK19 `mod.json` archives, generated `StreamingAssets/Mods/loadorder.json` through the reusable JSON load-order hook, and generic profile-order controls for managed load-order entries. Game-version validation, unmanaged external mod refresh, and historical staged-mod migrations remain blocked metadata until the shared runtimes exist.
- [x] `game-bloodstainedritualofthenight` - DMM counterpart: `internal/extensions/bloodstainedritualofthenight`. Source-backed installer parity covers Vortex `.pak` archive handling into `BloodstainedRotN/Content/Paks/~mods` plus alphabetic pak-folder load-order prefixes. Vortex unmanaged `.pak` adoption and historical migration behavior remain blocked metadata until DMM has the generic adoption/migration runtime.
- [x] `game-breakingwheel` - DMM counterpart: `internal/extensions/breakingwheel`. Source-backed parity covers Vortex's `ModdingTools` mod path and normal archive-root deployment.
- [x] `game-codevein` - DMM counterpart: `internal/extensions/codevein`. Source-backed installer parity covers Vortex `.pak` archive handling into `CodeVein/content/paks/~mods`, executable version metadata, and alphabetic pak-folder load-order prefixes. Vortex unmanaged `.pak` adoption and historical migration behavior remain blocked metadata until DMM has the generic adoption/migration runtime.
- [x] `game-conanexiles` - DMM counterpart: `internal/extensions/conanexiles`. Source-backed parity covers Vortex's `ConanSandbox/Mods` mod path, `.pak` stop-pattern archive installs, `ConanSandbox/Mods/modlist.txt` generation from enabled profile priority using the reusable DMM load-order-file helper, and generic profile-order controls for managed load-order entries. Vortex's explanatory mod-author guidance remains documented in extension metadata.
- [x] `game-cyberpunk2077` - DMM counterpart: `internal/extensions/cyberpunk2077`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-daggerfallunity` - DMM counterpart: `internal/extensions/daggerfallunity`. Source-backed installer parity covers Vortex `.dfmod` handling, Windows/no-platform payload filtering, and `DaggerfallUnity_Data/StreamingAssets` deployment.
- [x] `game-darkestdungeon` - DMM counterpart: `internal/extensions/darkestdungeon`. Source-backed installer parity covers Vortex `project.xml` mods, generated `project.xml` mods for no-project archives, game-directory structure matching, hero portrait folder routing, setup metadata for `mods`/`dlc`, and store launcher metadata.
- [x] `game-darksouls` - DMM counterpart: `internal/extensions/darksouls`. Source-backed parity covers Vortex `DATA/dsfix/tex_override` archive-root deployment, Steam launcher metadata, and a DSfix runtime requirement with a Nexus mod 19 acquisition prompt when the DSfix folder is missing.
- [x] `game-darksouls2` - DMM counterpart: `internal/extensions/darksouls2`. Source-backed parity covers both Vortex Steam app IDs, root archive deployment, and Vortex GeDoSaTo texture archives routed to `textures/DarkSoulsII` through the shared GeDoSaTo helper.
- [x] `game-dawnofman` - DMM counterpart: `internal/extensions/dawnofman`. Source-backed parity covers Vortex scenario `.scn.xml` installs into Documents scenarios, UMM-style `Info.json` installs into game `Mods`, setup metadata for the game `Mods` folder and Documents scenario folder, shared UMM tool archive staging/acquisition metadata, managed UMM tool discovery/default-primary launch metadata, and a runtime requirement for UMM-backed mods. Unity Mod Manager noninteractive patch execution remains inside UMM's own UI, matching Vortex.
- [x] `game-divinityoriginalsin2` - DMM counterpart: `internal/extensions/divinityoriginalsin2`. Source-backed parity covers Vortex Original and Definitive Edition registrations sharing Steam app `435150`, per-edition Proton Documents `Mods` target roots, archive-root `.pak` deployment, source-domain-aware install routing, setup metadata, GOG registry metadata, and the post-deploy in-game enable reminder.
- [x] `game-dmc5` - DMM counterpart: `internal/extensions/devilmaycry5`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-dragonage` - DMM counterpart: `internal/extensions/dragonage`. Source-backed parity covers Steam app metadata, Proton Documents override deployment, DAZIP outer/inner archive planning through the shared `dazip` helper, AddIns.xml generation during `will-deploy`, Steam launcher metadata, and setup metadata for Dragon Age Documents folders.
- [x] `game-dragonage2` - DMM counterpart: `internal/extensions/dragonage2`. Source-backed parity covers Steam app metadata, Proton Documents override deployment, DAZIP outer/inner archive planning into the game-root `addins` folder through the shared `dazip` helper, setup metadata, and Vortex's historical DAZIP purge migration through extension-declared state-migration commands.
- [x] `game-dragons-dogma` - DMM counterpart: `internal/extensions/dragonsdogma`. Source-backed installer parity covers Vortex nativePC archive routing, the Vortex invalid-archive confirmation installer, and Vortex's selective MT Framework ARC merge behavior for `game_main.arc` and `title.arc` through an extension-owned restore-aware `will-deploy` hook backed by the shared ARCtool bridge. Historical migration remains blocked until the shared migration runtime exists.
- [x] `game-elex` - DMM counterpart: `internal/extensions/elex`. Source-backed installer parity covers Vortex `.pak` archive handling into `data/packed`; FOMOD archives remain excluded exactly as the Vortex installer excludes them.
- [x] `game-enderal` - DMM counterpart: `internal/extensions/enderal`. Source-backed parity covers Vortex's `Data` mod path, Enderal executable metadata, Skyrim-family font settings diagnostics, and TES5Edit/Wrye Bash/FNIS/SKSE supported-tool metadata. Broader Enderal Gamebryo plugin/local-settings parity remains separate work.
- [x] `game-factorio` - DMM counterpart: `internal/extensions/factorio`. Source-backed installer parity covers Vortex's Linux `~/.factorio/mods` target root, executable metadata, setup metadata for ensuring the user mods folder, and default archive-root deployment into the Factorio mods folder.
- [x] `game-fallout3` - DMM counterpart: `internal/extensions/fallout3`. Source-backed parity covers Vortex Data-root installers, `fallout3ng.exe` executable preference with `fallout3.exe` fallback, FOSE installer/launch tool/runtime requirement, FOMOD choice metadata, FO3Edit/Wrye Bash supported tools, Gamebryo plugin activation, archive invalidation, Steam/GOG/Epic/Xbox metadata, and `Data/Fallout3.esm` hash-version detection through the shared hash provider.
- [x] `game-fallout4` - DMM counterpart: `internal/extensions/fallout4`.
- [x] `game-fallout4vr` - DMM counterpart: `internal/extensions/fallout4vr`. Source-backed parity covers Vortex `Data` root installers, F4SE VR script-extender installer/launch tool, Fallout 4 VR ESL-enabler routing, Gamebryo plugin activation metadata, archive invalidation target metadata, conflict-ignore metadata, and FO4VREdit supported-tool metadata. Dynamic ESL support toggled by an enabled `eslEnabler` mod remains a blocked generic plugin-activation capability.
- [x] `game-falloutnv` - DMM counterpart: `internal/extensions/falloutnv`. Source-backed parity covers Vortex `Data` root installers, New Vegas Script Extender installer/launch tool, 4GB patch `dinput` routing and metadata, Gamebryo plugin activation metadata, archive invalidation target metadata, Steam/Epic/Xbox launcher metadata, and FNVEdit/Wrye Bash supported-tool metadata.
- [x] `game-galciv3` - DMM counterpart: `internal/extensions/galacticcivilizations3`. Source-backed installer parity covers Vortex's broad archive installer, `.faction` routing to `Factions`, normal file routing to `Mods`, Crusade documents mod-type metadata, and deploy reminder.
- [x] `game-gardenpaws` - DMM counterpart: `internal/extensions/gardenpaws`. Source-backed parity covers Vortex Steam app metadata, `Mods` query-mod-path deployment, UMM game mod registration, UMM tool archive staging/acquisition/auto-acquire metadata, managed UMM tool discovery/default-primary launch metadata, and setup metadata for the Mods folder. Unity Mod Manager noninteractive patch execution remains blocked.
- [x] `game-greedfall` - DMM counterpart: `internal/extensions/greedfall`. Source-backed installer parity covers Vortex datalocal wrapper stripping, FOMOD exclusion, executable version metadata, and the did-deploy timestamp refresh hook.
- [x] `game-grimdawn` - DMM counterpart: `internal/extensions/grimdawn`. Source-backed parity covers Vortex `mods` archive-root deployment, setup metadata for the mods folder, GOG discovery metadata, and `Grim Dawn.exe`/`Game.dll` hash-version detection through the shared hash provider.
- [x] `game-grimrock` - DMM counterpart: `internal/extensions/grimrock`. Source-backed parity covers Vortex Documents Dungeons target-root deployment and setup metadata using the shared DMM `targetroots` helper.
- [x] `game-kenshi` - DMM counterpart: `internal/extensions/kenshi`.
- [x] `game-kerbalspaceprogram` - DMM counterpart: `internal/extensions/kerbalspaceprogram`. Source-backed parity covers Vortex's `GameData` mod path, Linux/Windows executable markers, normal archive-root deployment, and `Assembly-CSharp.dll` hash-version detection through the shared hash provider.
- [x] `game-kingdomcome-deliverance` - DMM counterpart: `internal/extensions/kingdomcomedeliverance`. Source-backed parity covers Vortex `Mods` deployment, transformed managed mod folders, `Mods/mod_order.txt` generation from profile priority, manual mod-order preservation on purge, Steam/Epic/Xbox launcher metadata, generic profile-order controls for managed load-order entries, and collection-data metadata. Collection import/export runtime remains blocked.
- [x] `game-masterchiefcollection` - DMM counterpart: `internal/extensions/masterchiefcollection`.
- [x] `game-microsoftflightsimulator` - DMM counterpart: `internal/extensions/microsoftflightsimulator`. Source-backed parity covers Steam/MS Store metadata, Proton LocalCache/UserCfg.opt Community path resolution, Community package installers, official-file replacer installers, target-choice prompts for ambiguous replacers, generated layout entries for replacer packages, setup metadata, merge/load-order metadata, and copy deployment defaults. Vortex aircraft.cfg merging, `.locPak` localization merging, generated merged package layout maintenance, and load-order folder prefix semantics remain blocked until DMM ports the full MSFS merge runtime.
- [x] `game-monster-hunter-world` - DMM counterpart: `internal/extensions/monsterhunterworld`. Source-backed installer parity covers Vortex `nativePC` archive stripping, ReShade `.ini` deployment to the game root with the same ReShade warning, Stracker's Loader root-file routing, setup metadata for `nativePC`, and HunterPie/SmartHunter/MHW Transmog supported-tool metadata.
- [x] `game-morrowind` - DMM counterpart: `internal/extensions/morrowind`. Source-backed parity covers Vortex Steam/GOG/MS Store metadata, Data Files root/folder installers, FOMOD Data Files root metadata, TES3Edit and Construction Set supported-tool metadata, restore-aware `Morrowind.ini` Game Files generation, ESP/ESM timestamp ordering, generic profile-order controls for managed plugin entries, and plugin-list attribute metadata. Filesystem watcher state and collection import/export runtime remain blocked.
- [x] `game-mount-and-blade` - DMM counterpart: `internal/extensions/mountandblade`. Source-backed installer parity covers the three Vortex-registered variants, `module.ini` module-package installs using the archive name as the module folder, supported loose override file routing into the native module, and native module version discovery.
- [x] `game-mount-and-blade2` - DMM counterpart: `internal/extensions/mountandblade2bannerlord`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-nehrim` - DMM counterpart: `internal/extensions/nehrim`. Source-backed installer parity covers Vortex's Nehrim app `1014940`, Oblivion app `22330` environment, Nehrim Launcher supported tool, copy-only deployment requirement, `data` mod path, and cross-app deployment into Oblivion's Steam install root through the reusable Steam app target-root resolver.
- [x] `game-neverwinter-nights` - DMM counterpart: `internal/extensions/neverwinter`. Source-backed installer parity covers classic NWN and NWN: Enhanced Edition loose extension routing, override-folder preservation, structured Neverwinter folder archives, and the EE Documents mod root.
- [x] `game-neverwinter-nights2` - DMM counterpart: `internal/extensions/neverwinter`. Source-backed installer parity covers `.mod` module archives routed to the Documents modules folder and registers the Vortex-declared override mod type as a Documents override target.
- [x] `game-nomanssky` - DMM counterpart: `internal/extensions/nomanssky`. Source-backed parity covers Vortex's `GAMEDATA/MODS` default path, deprecated `.pak` deployment into `GAMEDATA/PCBANKS/MODS`, engine-injector `.dll` deployment into `Binaries`, Xbox launcher metadata, Vortex-compatible `Binaries/NMS.exe` ProductVersion parsing through the generic PE version provider, setup metadata for enabling PCBANKS mods, and the Vortex 1.0.1 purge-retag-redeploy migration through extension-declared migration commands.
- [x] `game-oblivion` - DMM counterpart: `internal/extensions/oblivion`. Source-backed parity covers Vortex Data-root installers, OBSE installer/launch tool/runtime requirement, FOMOD choice metadata, TES4Edit/Wrye Bash supported tools, Gamebryo plugin activation, archive invalidation, Oblivion INI missing-font diagnostics and automatic font repair, Steam/GOG/Xbox metadata, and `Data/Oblivion.esm` hash-version detection through the shared hash provider.
- [x] `game-oni` - DMM counterpart: `internal/extensions/oxygennotincluded`. Source-backed parity covers Vortex Steam app metadata, `Mods` query-mod-path deployment, UMM game mod registration, UMM tool archive staging/acquisition/auto-acquire metadata, managed UMM tool discovery/default-primary launch metadata, and setup metadata for the Mods folder. Unity Mod Manager noninteractive patch execution remains blocked.
- [x] `game-palworld` - DMM counterpart: `internal/extensions/palworld`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-pathfinderkingmaker` - DMM counterpart: `internal/extensions/pathfinderkingmaker`. Source-backed parity covers Vortex Steam app metadata, `Mods` query-mod-path deployment, UMM game mod registration, UMM tool archive staging/acquisition/auto-acquire metadata, managed UMM tool discovery/default-primary launch metadata, and setup metadata for the Mods folder. Unity Mod Manager noninteractive patch execution remains blocked.
- [x] `game-pathfinderwrathoftherighteous` - DMM counterpart: `internal/extensions/pathfinderwrathoftherighteous`. Source-backed parity covers Vortex Steam and GOG metadata, `Mods` query-mod-path deployment, UMM game mod registration, UMM tool archive staging/acquisition metadata, `autoDownloadUMM` active-game auto-acquire metadata, setup metadata for the Mods folder, and Vortex's `Wrath_Data/StreamingAssets/Version.info` version provider. The Unity Mod Manager noninteractive patch runtime remains blocked.
- [x] `game-pillarsofeternity2` - DMM counterpart: `internal/extensions/pillarsofeternity2`. Source-backed parity covers Vortex Steam/MS Store metadata, dynamic Steam/MS Store override folder resolution, LocalLow `modconfig.json` setup, manifest-root archive routing, `SupportedGameVersion` manifest metadata extraction, restore-aware `modconfig.json` generation from profile priority, and generic profile-order controls for managed load-order entries.
- [x] `game-prisonarchitect` - DMM counterpart: `internal/extensions/prisonarchitect`. Source-backed installer parity covers Vortex LocalAppData `Introversion/Prison Architect/mods` deployment adapted to Steam Deck Proton LocalAppData and `Launcher/launcher-settings.json` version parsing. Native Linux mod storage remains blocked until source-verified.
- [x] `game-re2remake` - DMM counterpart: `internal/extensions/residentevil22019`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-re3remake` - DMM counterpart: `internal/extensions/residentevil32020`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-rimworld` - DMM counterpart: `internal/extensions/rimworld`.
- [x] `game-sekiro` - DMM counterpart: `internal/extensions/sekiro`. Source-backed installer parity covers Vortex loose `.partsbnd.dcx` handling, root `parts` package handling, and Sekiro Mod Engine diagnostics.
- [x] `game-shadowrunreturns` - DMM counterpart: `internal/extensions/shadowrunreturns`. Source-backed parity covers Vortex ContentPacks archive-root deployment, Shadowrun editor supported-tool metadata, setup metadata, and managed-assembly/editor hash-version detection through the shared hash provider.
- [x] `game-sims3` - DMM counterpart: `internal/extensions/sims3`. Source-backed parity covers Vortex Documents Packages target-root deployment adapted to the Steam Deck Proton Documents path, Resource.cfg setup metadata, Windows registry metadata, and Vortex-compatible `game/bin/skuversion.txt` `GameVersion` parsing through the generic text-file game-version provider.
- [x] `game-sims4` - DMM counterpart: `internal/extensions/sims4`. Source-backed parity covers Vortex localized Electronic Arts user-data resolution adapted to Steam Deck Proton Documents, mixed Tray/Mods archive routing, `Mods/Vortex Mods` deployment, Resource.cfg patch generation, Options.ini mod/script enablement, 32-bit executable supported-tool metadata, and legacy Vortex Mods migration metadata.
- [x] `game-skyrim` - DMM counterpart: `internal/extensions/skyrim`. Source-backed parity covers Vortex Data-root installers, SKSE installer/launch tool/runtime requirement, FOMOD choice metadata, TES5Edit/Wrye Bash/FNIS/BodySlide supported tools, BodySlide x64 executable preference with BodySlide.exe fallback, Gamebryo plugin activation, archive invalidation, Skyrim font settings diagnostics, and Steam Workshop coexistence metadata.
- [x] `game-skyrimse` - DMM counterpart: `internal/extensions/skyrimse`. Source-backed parity covers Vortex `Data` root installers, SKSE64 script-extender installer/launch tool/runtime requirement, FOMOD choice metadata, Gamebryo plugin activation, archive invalidation, local game settings, Skyrim font settings diagnostics, game-version detection, and supported-tool metadata.
- [x] `game-skyrimvr` - DMM counterpart: `internal/extensions/skyrimvr`. Source-backed parity covers Vortex `Data` root installers, SKSEVR script-extender installer/launch tool, Skyrim VR ESL-enabler routing, Gamebryo plugin activation metadata, archive invalidation target metadata, Skyrim font settings diagnostics, and TES5VREdit/FNIS supported-tool metadata. Dynamic ESL support toggled by an enabled `eslEnabler` mod remains a blocked generic plugin-activation capability.
- [x] `game-spyroreignitedtrilogy` - DMM counterpart: `internal/extensions/spyroreignitedtrilogy`.
- [x] `game-starbound` - DMM counterpart: `internal/extensions/starbound`. Source-backed parity covers Vortex `mods` archive-root deployment, setup metadata for the mods folder, Xbox launcher metadata, and the `ModifiableWindowsApps` Xbox executable variant with source-backed path and file predicates.
- [x] `game-stardewvalley` - DMM counterpart: `internal/extensions/stardewvalley`.
- [x] `game-starfield` - DMM counterpart: `internal/extensions/starfield`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata. Vortex declares root `queryModPath` metadata only; DMM keeps this as metadata until source-reviewed installer support exists.
- [x] `game-stateofdecay` - DMM counterpart: `internal/extensions/stateofdecay`. Source-backed parity covers Vortex `game` archive-root deployment and Steam app metadata.
- [x] `game-subnautica` - DMM counterpart: `internal/extensions/subnautica`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata and `QMods` query path metadata. The upstream Vortex game extension does not define QMods archive installation behavior, so DMM keeps this as support-mod metadata instead of inventing executable support.
- [x] `game-subnauticabelowzero` - DMM counterpart: `internal/extensions/subnauticabelowzero`. Source-backed parity covers the Vortex `registerGameStub` support-mod metadata and `QMods` query path metadata. The upstream Vortex game extension does not define QMods archive installation behavior, so DMM keeps this as support-mod metadata instead of inventing executable support.
- [x] `game-survivingmars` - DMM counterpart: `internal/extensions/survivingmars`. Source-backed installer parity covers Vortex `modcontent.hpk` archive handling into the Proton AppData mods path.
- [x] `game-sw-kotor` - DMM counterpart: `internal/extensions/kotor`. Source-backed installer parity covers KOTOR and KOTOR II, game-root folder archives, default override-folder archives, and explicit blocked TSLPatcher tool/mod payloads matching Vortex's refusal to install those as normal mods.
- [x] `game-teamfortress2` - DMM counterpart: `internal/extensions/teamfortress2`. Source-backed installer parity covers Vortex `.vpk` archive handling into `tf/custom`, Hammer supported-tool metadata, and `tf/steam.inf` ClientVersion discovery.
- [x] `game-teso` - DMM counterpart: `internal/extensions/teso`. Source-backed parity covers Vortex Documents AddOns target-root deployment adapted to the Steam Deck Proton Documents path, Nexus `elderscrollsonline` domain metadata, standalone launcher registry metadata, setup metadata, and launcher/game version hash detection through the shared hash provider.
- [x] `game-torchlight2` - DMM counterpart: `internal/extensions/torchlight2`. Source-backed installer parity covers Vortex `.mod` archive handling into the documents mods path and records the Steam launcher requirement metadata.
- [x] `game-totalwarthreekingdoms` - DMM counterpart: `internal/extensions/totalwarthreekingdoms`. Source-backed installer parity covers Vortex `.pack` archive handling by copying files from the folder containing the first `.pack` into `data`, Assembly Kit supported-tool metadata, Steam/Epic/GOG launcher metadata, setup metadata for the data folder, and a deploy reminder.
- [x] `game-untitledgoose` - DMM counterpart: `internal/extensions/untitledgoose`. Source-backed parity covers Vortex's Epic-only game metadata, BepInEx plugin/root/config/runtime installers through the shared BepInEx helper, runtime requirement metadata, source-backed BepInEx runtime acquisition through the captured-install pipeline, setup metadata for BepInEx folders/config, and blocked metadata for Epic discovery and the Vortex 0.2.0 migration purge.
- [x] `game-vtmbloodlines` - DMM counterpart: `internal/extensions/vtmbloodlines`. Source-backed parity covers Vortex Steam/GOG metadata, Steam launcher metadata, `Vampire` and `Unofficial_Patch` setup folders, default Vampire-root deployment, explicit Unofficial Patch root routing, and `version.inf` ExtVersion parsing.
- [x] `game-warthunder` - DMM counterpart: `internal/extensions/warthunder`. Source-backed installer parity covers Vortex's `UserSkins` default deployment, `.fsb` audio deployment into `sound/mod`, Steam executable metadata, setup metadata for the mod folders, and the Vortex `config.blk` audio `sound{}` patch through the reusable text patch event hook.
- [x] `game-witcher` - DMM counterpart: `internal/extensions/witcherlegacy`. Source-backed installer parity covers Vortex's `Data/Override` default mod path, `cook.hash` user-content detection, Steam executable metadata, and setup metadata for the Override folder.
- [x] `game-witcher2` - DMM counterpart: `internal/extensions/witcherlegacy`. Source-backed installer parity covers Vortex's Linux Steam executable/required-file metadata, `CookedPC` default mod path, `cook.hash` user-content deployment into `UserContent`, and setup metadata for the UserContent folder.
- [x] `game-witcher3` - DMM counterpart: `internal/extensions/witcher3`. Source-backed parity covers Vortex Steam/Nexus IDs, mod/DLC/menu installer shapes, managed `mods.settings` generation, Script Merger supported-tool metadata and deploy reminders, deploy-ignore/conflict-ignore patterns for menu fragments, and Vortex-style menu `.part.txt` settings merge into Proton Documents through restore-aware extension-generated mappings. Remaining gaps are advanced menu XML merge, Script Merger execution/setup parity, collection/profile merged-data import/export, and full manual load-order UI semantics.
- [x] `game-wolcen` - DMM counterpart: `internal/extensions/wolcen`. Source-backed parity covers Steam app metadata, `Game` query-mod-path archive deployment with Vortex stop patterns, setup metadata for the `Mods` folder, and XML/MTL merge generation through the reusable `internal/extensions/xmlmerge` helper.
- [x] `game-worldoftanks` - DMM counterpart: `internal/extensions/worldoftanks`. Source-backed installer parity covers Vortex's dynamic `res_mods/<version>` target root resolved from `version.xml`, required executable/version metadata, and default archive-root deployment into that versioned target.
- [x] `game-x4foundations` - DMM counterpart: `internal/extensions/x4foundations`.
- [x] `game-xcom2` - DMM counterpart: `internal/extensions/xcom2`. Source-backed parity covers Vortex Steam/GOG/Epic metadata, base XCOM 2 and War of the Chosen mod/config roots, `.XComMod` archive routing, base/WOTC variant detection from the installed game path, DefaultModOptions.ini generation from profile priority, Steam Workshop coexistence metadata, launcher/ModBuddy supported-tool metadata, setup metadata for mod/config folders, and generic profile-order controls for managed load-order entries. Vortex's separate logical-game UI remains blocked until DMM has a generic same-Steam-app variant selector.
- [x] `game-xrebirth` - DMM counterpart: `internal/extensions/xrebirth`. Source-backed parity covers `content.xml` installers, X Rebirth stop-pattern/drop-in installers, savegame/shader/utility/save-patch/documentation mod types, and the Vortex mod health checks for empty output, content.xml metadata, and recognisable mod shape through DMM diagnostics.
