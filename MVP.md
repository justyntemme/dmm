Review `GUIDELINES.md` before implementing. Keep Go code idiomatic, keep tests lightweight, prefer live Deck/API verification over elaborate mocked environments, and commit focused code changes as features complete. Planning docs are working notes and should not be committed unless explicitly requested.

Legend:
- `✓` means complete and live/user verified enough to count.
- `△` means implemented or partially implemented, but still needs product polish, live validation, or scope correction before MVP approval.
- `□` means not complete.

MVP Items

△ 98% Expand remote-provider/source architecture beyond Nexus.
  User decision: popular non-Steam upstream providers are now MVP scope. Nexus remains the first implemented provider, but DMM must be shaped so ModDB, Thunderstore, GitHub Releases, Modrinth, GameBanana, direct URL, local archive, and similar popular non-Steam sources can be added without rewriting captured installs, profile mods, updates, or deployment.
  Required behavior: every mod row carries and displays source identity as a small source pill. Source/catalog identity must persist through capture, cache, local install, profile state, update checks, removal, rollback/history, jobs, and UI filtering/sorting.
  Verification rule: do not guess provider behavior. Inspect official API docs, source, client behavior, schemas, or real fixtures before implementing a provider.
  Steam Workshop scope: do not build a Steam Workshop browsing/search UI for MVP. Treat Workshop as Steam-owned platform content; DMM should display installed/subscribed Workshop mods alongside other mods where safe and manage disable, enable, unsubscribe/remove, and load-order movement only through verified Steam/Decky APIs.
  Current state: Nexus, Thunderstore, GitHub Releases, Modrinth, GameBanana, direct URL, local archive, key-gated mod.io, and key-gated CurseForge all use the shared captured-install/download-or-upload/profile pipeline. ModDB remains deferred until a supported automated API/client path is verified. Web and Decky installed mod rows, Nexus browse/search result rows, Action Center rows, installer-choice rows, job/activity rows, file-winner conflict choices, and Steam Workshop rows show source pills. Resolve-only jobs now persist catalog/app/game/mod/file payloads too, so source tags survive into the Jobs panel. Install-candidate list/save/apply/retry responses now use explicit API DTOs with normalized `source_tag` fields instead of leaking raw storage rows. Live Deck validation installed a verified GitHub Releases Stardew archive (`StardewValleyMods/CloudSync`) through the shared captured-install path as a disabled `stardew-smapi-mod` with `source_tag=github`, and the deploy preview contained no CloudSync mappings while disabled. Phone/tablet Action Center and Jobs now include source filters built from the same normalized source tags used by mod rows. Phone/tablet Mods now renders Steam Workshop rows in the normal profile-management surface, while Review keeps only coexistence diagnostics. Update checks now route through provider capabilities: Nexus remains first-class, GitHub Releases/Modrinth/Thunderstore/GameBanana/mod.io/CurseForge can resolve latest/specific files through their verified resolver paths, and Direct/Local/Steam Workshop persist explicit unsupported status instead of disappearing from the UI. Steam Workshop browse/search is intentionally out of MVP; management is installed-content only through the verified Decky/Steam API boundary. Latest local passes added explicit metadata-only extension coverage for installed games with verified ModDB/GameBanana pages but no safe automation path: (the) Gnorp Apologue, Braid, FEZ, Heavy Bullets, Hotline Miami, Mark of the Ninja, McPixel, Nuclear Throne, PlanetSide 2, Shieldwall, and STAR WARS: Rogue Squadron 3D. These do not claim Nexus, Workshop, or archive installer support; they exist so DMM can explain known source state without inventing unsupported capabilities. Phone/tablet and Decky UIs now show metadata-only known source references explicitly instead of presenting them as missing Nexus configuration. Latest live provider resolver validation passed for direct, GitHub Releases, Thunderstore, Modrinth, and GameBanana URL resolution; mod.io and CurseForge correctly reported `needs_credentials`, and ModDB remained explicitly deferred.

✓ Native Linux Stardew Valley vertical slice is working as the reference behavior for the current code path.
  Verified state: Nexus automatic downloads can be captured, SMAPI can be installed from Nexus, SMAPI mods can be staged/enabled/deployed, Steam launch options were changed through the Decky/Steam API path, Stardew launched through SMAPI, and a tested SMAPI mod loaded in-game. Live diagnostics now show `SMAPI: ok`, `SMAPI launch: ok`, and the deployment preview no longer shows false copied-file replacements after apply.
  Remaining scope correction: this proves the Stardew slice works, but it does not prove the extension architecture is complete or Vortex-parity complete.

✓ Backend starts automatically when the Decky plugin loads.
  Verified state: after package install and Deck reboot, the Decky plugin autostarted the Go backend without manually pressing `Start Server`.

△ 70% Add MVP API auth and phone pairing.
  Required behavior: Decky generates a process-scoped API token when it launches the backend, stores it only in a user-readable runtime file, passes it to the Go backend through environment, and uses it for Decky bridge requests, the direct event WebSocket, and the OS `nxm://` handler. The phone/tablet URL carries the token as a URL fragment for first pairing; the web app stores it locally, removes it from the visible URL, and sends it as `X-DMM-Token` plus WebSocket query token. `/api/health` may remain unauthenticated, but all other API/read-model/mutation/event routes must reject requests without the token when auth is enabled.
  Security boundary: token auth is additive to LAN-only filtering, not a replacement. LAN-only remains enabled by default and can still be disabled for trusted VPN/tunnel use. Logs must not include the token.
  Remaining: live package validation, visible Decky security copy, and a reset/session-rotation control before MVP approval.

✓ Replace frontend polling with a backend event bus and WebSocket delivery as the main realtime architecture.
  Verified state: the phone/tablet UI saw new requests immediately during live testing, and Decky connects a background event monitor while the plugin is loaded.
  Remaining polish: individual UI panes still need targeted refresh/state handling so every click visibly updates the current pane without navigation or manual refresh.

✓ Deployment planner supports copied launch/runtime files without leaving false pending profile changes.
  Verified state: copied SMAPI launch/runtime files now compare checksums and report `keep` after deployment instead of fake `replace` changes.

△ 92% Fix realtime phone/tablet UI updates for every affected pane.
  Must cover: Action Center entries, approve/install actions, jobs, profile changes, deployment state, runtime actions, button in-flight states, and error recovery.
  Current live feedback: requests now appear quickly, but approving a request previously left stale UI state until navigating/refreshing. Current implementation note: websocket events now coalesce action-state refreshes so jobs, global installer candidates, and the selected game reload through one path instead of racing separate job/candidate/game refreshes. Captured-install approve/retry/cancel buttons now optimistically transition the card to a processing state immediately and force a backend refresh on error. FOMOD candidate apply/retry also removes the local candidate immediately when the backend returns an installed mod. The backend now exposes a global install-candidates read model at `/api/install-candidates`, and the phone/tablet landing Action Center refreshes jobs plus installer candidates together so blocked/FOMOD candidates remain visible even when no game is selected or the companion installer-choice job is no longer present. Phone/tablet installer-choice saves now replace the local candidate with the backend-reevaluated candidate response so conditional FOMOD pages can update without a manual refresh. Websocket `jobs.snapshot` reconnects now trigger the same action-state refresh path, so global installer candidates cannot stay stale after a reconnect. The newest web pass also refreshes global Action Center state from action-related WebSocket events even when no game is selected and stops failed installer-choice jobs from hiding their persisted review candidates on the landing Action Center. Failed captured-install actions can now be dismissed with the same cancel path as active actions; the live Deck package canceled stale failed Fallout install actions without touching game files. Needs live phone/tablet verification before completion.

△ 99% Finish FOMOD core support.
  Required behavior: persisted choice sessions, resume after choices, phone/tablet choice UI, no silent auto-enable, and installed mod remains disabled unless explicitly enabled.
  Current state: code exists for FOMOD/installer-choice sessions, and FOMOD application now routes target roots/mod types through extension-registered installer-choice capabilities instead of generic `mod_type=fomod`. Fallout 4 and Skyrim SE register Vortex-verified `Data` roots plus Gamebryo stop-folder metadata, so wrapped installer sources such as `Wrapper/Data/...` and `Wrapper/meshes/...` normalize to the intended game Data targets. FOMOD parser now handles selected-plugin condition flags and applies matching `conditionalFileInstalls` patterns. FOMOD file dependencies are now evaluated through explicit file-state maps or a server-provided target-state resolver; filesystem presence is treated as `Active`, absence as `Missing`, and extension-declared plugin activation state can now classify DMM-managed disabled Gamebryo plugins as `Inactive`. FOMOD `gameDependency` predicates now parse as minimum-version checks and consume a semantic game version supplied by the extension/core game-version contract rather than Steam build IDs. FOMOD `dependencyType` plugin type rules now parse and evaluate sequentially, so default selections and explicit choice validation honor Vortex-style dynamic `Required`/`Recommended`/`NotUsable` state from earlier flags plus file/game dependency state. Installer-choice candidates now persist backend-computed default `choices_json`, so phone/tablet and Decky UIs start from the server's dynamic evaluator instead of stale static client defaults. FOMOD install-step visibility now parses `<visible>` dependency blocks; the backend evaluates visible steps from the current/default selections and both phone/tablet and Decky UIs hide steps marked `visible: false`. FOMOD choice saves now return and persist backend-reevaluated installer metadata, and both phone/tablet and Decky choice surfaces consume that response so conditional steps update immediately after a selection. Dynamic option state is serialized separately as `effective_type`, preserving the parsed/default `type` and preventing stale Required/NotUsable state from leaking into later re-evaluations after the user changes an earlier wizard answer. FOMOD file entries now parse and honor `alwaysInstall`, `installIfUsable`, and file priority ordering so higher-priority archive entries stage after lower-priority entries. FOMOD `moduleDependencies` now parse and validate file/game/FOMM-host version dependencies before staging; DMM exposes a generic FOMOD host compatibility version through `PlanOptions` mirroring Vortex's `getAppVersion` callback boundary. FOMOD `order` attributes on install steps, groups, and plugins now apply schema-compatible `Ascending`/`Descending`/`Explicit` ordering before DMM assigns stable choice IDs. DMM now detects nested `.fomod` packages, extracts the inner archive into the install workspace, and routes it through the same extension-owned FOMOD planner, matching Vortex's nested FOMOD delegation shape. The phone/tablet and Decky choice UIs now mirror Vortex's wizard-style step progression instead of one giant form, include a Vortex-style `None` radio for `SelectAtMostOne`, lock Required/NotUsable options while backend validation remains the authority, and now render generic extension-owned text prompts through the same persisted choice path. Ghost Recon Breakpoint uses that text-choice capability for Vortex-verified `.data` -> `.forge` staging prompts. Do not mark complete until a real Nexus FOMOD flow is tested.
  Live validation note: a direct DMM Nexus HTTPS file capture for a tiny Fallout 4 FOMOD (`fallout4/mods/90384/files/344008`) reached Nexus but the non-premium API refused download links without a browser-generated `nxm://` key. DMM now reports that cleanly and tells the user to click Mod Manager Download in the Deck browser. The Stardew `Neural Harvest` archive live-tested the Decky modal display path before DMM had required-source preflight, but its own FOMOD metadata references missing `Common/...` files that are not present in the zip. DMM now validates always-required FOMOD sources before creating installer-choice jobs, documents Neural Harvest as a malformed negative fixture, and persists it as a blocked review-only item with a clear reason. Latest package also keeps blocked items review-only in the phone/tablet Action Center instead of showing installer/profile controls that imply they can proceed. This does not complete the FOMOD live test; it confirms the remaining live path must use a real browser `nxm://` capture for a valid FOMOD archive or a premium-capable API account.
  Latest live validation: Fallout 4 Pip-Boy Flashlight (`fallout4/mods/10840/files/65415`) installed successfully from the cached browser-captured Nexus archive. DMM now mirrors Vortex-native FOMOD tolerance by skipping missing selected choice sources such as absent `README` folders with warnings, while missing `requiredInstallFiles` remains fatal. The same package adds explicit installer reconfiguration: normal reinstall reuses saved choices, while Decky `Menu Reconfigure` and the phone/tablet `Reconfigure` button force the installer-choice modal back open from the cached archive.

△ 80% Add Decky modal/no-phone FOMOD flow.
  Desired UX: if a Deck-side download reaches installer choices and auto-install is enabled, DMM should try to open a Decky-native modal automatically. If Decky cannot safely display a background modal while Firefox is open, show a Decky notification telling the user to open Decky Mod Manager and continue from Action Center.
  Current state: Decky listens to backend WebSocket events in the background, opens a FOMOD installer-choice modal when an `installer-choice` job enters `waiting`, and now exposes a Deck-side "Auto-display FOMOD installers" setting enabled by default. Closing an automatically opened modal records an in-memory dismissal so reconnect/snapshot events do not reopen the same waiting installer immediately; the item remains in Action Center and manual open still works. The modal now replaces its local candidate after saving choices with the backend-reevaluated candidate response, so conditional installer pages can appear/disappear without reopening the modal. The modal now advances one evaluated FOMOD step at a time with Back/Next/Apply controls. Malformed FOMOD archives with missing always-required sources now block before the modal instead of opening a wizard that cannot succeed. Treat as incomplete until a valid FOMOD archive is installed successfully through the Firefox/Nexus `nxm://` flow.
  Latest deployed fix: after `Apply Choices`, the Decky modal suppresses the same candidate while the backend apply job is running, preventing the stale waiting event from reopening the wizard before the candidate is removed or marked terminal.

△ 80% Redesign phone/tablet mod UI around profile-first management.
  Required UX: ordinary users see installed mods in the selected profile with simple enable/disable/remove/reinstall controls. Staging, preview, purge, and repair are advanced/power-user surfaces. Download/install approvals should live in Action Center, not as low-level staging workflow.
  Current state: improved but not final. Primary game mod screen now labels the selected profile directly, keeps enable/disable as the ordinary control, and renames the manual advanced apply action to profile language instead of pipeline language. The latest web pass changes Action Center approval wording to simple install language and updates profile summary text so users are pointed at enable/disable behavior while file-level apply details stay in Advanced Deployment Tools. Phone/tablet mod rows now expose the normal lifecycle controls directly: enable/disable, reinstall from cache, and remove. Source IDs and profile ordering remain under Advanced. The selected-profile summary now keeps Action Center visibly reachable even when there are zero open actions. Healthy installed mods now report `status: "installed"` through the API instead of leaking `status: "staged"` into normal UI state; this was package-deployed and live-verified against Stardew on the Deck. Current committed pass adds an active-profile selector in the Mods screen, install-to-profile targeting for URL/local/archive/installer-choice flows, profile copy/move controls in Advanced, and profile-only remove semantics that keep cached/staged installed mods recoverable. Backend storage now requires source-profile membership before profile move/remove operations. Added `vfs.md` to explain DMM-owned storage, symlink deployment, profile transitions, conflicts, repair, and rollback. New profile rows/API responses show accurate profile mod counts, invalid stale profile membership rows are ignored and cleaned on startup, profile selectors show `on / total` context, and the Profiles tab can seed a new profile from the active loadout with matching enabled state and priority. Captured-install jobs, restored pending installs, update jobs, installer-choice jobs, and Action Center profile defaults now preserve the target profile ID so delayed approvals do not silently fall back to the currently viewed profile. Live Deck checks passed for profile transfer, seeded profile creation, invalid membership cleanup, one-click enable/disable/apply, MVP Stardew acceptance, visible DMM-managed Stardew symlinks, rollback restore, and all-current Stardew update checks. Latest tablet web screenshot pass fixed the Add Mod Nexus search controls so the input no longer collapses in the two-column profile layout. User explicitly said the broader UI redesign can wait until tomorrow, but it remains MVP before approval.

△ 96% Finish Decky-side game selector UX.
  Live feedback: game entries in the Decky plugin do not visibly highlight while navigating with the D-pad, so the user cannot tell which game is selected. The list may be focusable internally, but it is not visually communicating focus.
  Latest live feedback: after navigating down into the game list with the D-pad, the scroll container does not allow returning far enough upward to fully reveal the tab bar. The L1/R1 tab navigation still works, but the tabs are mostly clipped, which makes the panel feel broken. Fix the scroll/focus layout so the active tab strip and the current list context remain reachable and visible after long D-pad navigation.
  Latest controller feedback: D-pad up/down sometimes skips visible elements instead of moving one row at a time, and B does not reliably back out of nested Nexus mod explorer/list-more states. Touchscreen success does not count as MVP completion here; Gaming Mode controller navigation must be deterministic, visually obvious, and recoverable without trapping the user in a modal or long result list.
  Required fix: game rows must use the same clear selected/focused styling as the tab list, support D-pad navigation, support touch/click, and remain readable at Decky sidebar width.
  Required controller acceptance: audit the Decky `Focusable` hierarchy, scroll containers, selected row state, nested Nexus modal states, result pagination/list-more state, `onCancelButton` handling, and preferred focus positions so each D-pad press reaches the adjacent visible control and B backs out one nested state before leaving DMM.
  Required controls: search bar above the list, sort dropdown, favorites pinned to top, and recent-game ordering.
  Current implementation note: latest package removes the nested scroll body, removes custom virtual D-pad steering/windowed rendering, makes tabs visibly focusable, and simplifies game rows to one native Decky focus target per row. A selects the focused game and Y toggles favorite. A follow-up package adds explicit selected-game focus ownership after a clicked game row unmounts, so D-pad down should enter the selected game content instead of returning to the native tab header/Manage tab. Package installed/rebooted on the Deck and backend autostart verified. The newest Decky package also keeps extra bottom padding for Steam's button-hint bar and compacts the Manage tab into one bounded surface so the paste-link action is not covered. Decky event refresh churn was reduced by narrowing Steam launch/workshop action sync triggers and throttling routine Workshop state sync per game while preserving forced sync after actual Workshop mutations. Needs live Gaming Mode validation before completion.

△ 96% Use one shared backend source of truth for game UI preferences.
  Live feedback: Stardew was favorited in the phone/tablet web app but did not appear favorited in Decky. This proves Decky and web favorites are not using the same persisted source.
  Required fix: add backend API/storage for favorite games, sort order, and recents. Both the Svelte web UI and Decky plugin must read/write the same endpoints. Do not keep separate local-only preference stores.
  Current implementation note: backend now exposes `GET/PATCH /api/settings/ui`, emits `ui.changed`, and both web and Decky send intent-level patches rather than whole preference snapshots. `go test ./...`, web build, Decky build, package install, backend autostart, and live API patch validation passed. `testing/live_ui_preferences_check.sh` passes against the Deck after the latest deployed package by round-tripping favorites, sort order, and recents through the shared backend API. Needs visual web-to-Decky / Decky-to-web repaint validation before completion.

✓ Filter Steam runtimes/tools out of user game lists.
  Live feedback: Steam Linux Runtime, Proton runtimes, Steamworks redistributables, and similar helper apps appear in the game list. These are not useful mod targets for ordinary users.
  Required fix: filter known Steam runtime/tool app IDs and app types from primary game selectors. Keep a debug/advanced visibility path if useful for diagnostics.
  Verified state: Steam discovery and storage-backed game reads now hide known Valve helper AppIDs and clear helper naming patterns without deleting old rows. Unit coverage verifies Proton/Steam runtime filtering and normal game preservation. Live `/api/games` dropped from 96 to 86 entries with `helper_count 0`, status `game_count` now reports 86, and the Stardew MVP live check still passes.

△ 88% Finish Decky-side mod management UX.
  Live feedback: after selecting a game, mod rows are too wide and clip content/actions. The user can infer some controls are focusable because right-arrow moves between side-by-side actions, but the focused mod row itself is not highlighted clearly.
  Latest live feedback: Stardew's `Mod Menu` entry shows an extension/manifest-related error and appears to break navigation or scrolling around the mod list. After moving down the mod list, the user cannot scroll back far enough to reach `Change Game` or the top controls. Error-state mod rows must remain fully focusable, bounded, and navigable, and the row should present a clear recovery message/action instead of trapping the focus/scroll area.
  Required fix: rows must fit within Decky horizontal bounds, avoid clipped text, expose visible enable/disable/delete/reinstall controls, and make the focused row/action obvious with D-pad navigation.
  Design decision to investigate: whether Decky allows a wider plugin panel. If widening is official/safe, evaluate it. If it creates inconsistent or unsupported behavior, keep the standard width and redesign rows into compact stacked controls.
  Current implementation note: standard sidebar remains the target; dense workflows should use wider modals rather than unsupported Quick Access panel widening. Latest package simplifies Action Center, mod, and Workshop rows to one native Decky focus target per row. For installed mods, A toggles enable/disable, Y reinstalls, and Options/Menu removes. This removes the nested row/action focus model that was likely trapping scroll. Decky now avoids unrelated launch/workshop syncs on generic job updates, and routine selected-game refreshes only ask SteamClient for Workshop state when the selected extension supports Workshop and the per-game throttle permits it. Current local pass changes Decky remove to call the profile-scoped remove route, matching the UI text and keeping cached/staged files recoverable instead of deleting the installed mod globally. The obsolete game-level delete endpoint and Decky bridge path were removed. Package built, installed through the privileged wrapper, rebooted, package parity passed, and `/api/health` returned ok. Needs live D-pad/overflow/performance/profile-remove validation before completion.

△ 80% Fix Decky B-button/internal back behavior.
  Live feedback: pressing `B` after selecting a game backs out of the entire Decky plugin instead of returning to the previous internal state or game list. This is frustrating because the user expects local navigation before leaving DMM.
  Required fix: investigate Decky controller/back APIs and implement an internal back affordance where possible. If Decky owns `B` globally and it cannot be intercepted safely, add an obvious controller-focusable Back row/button inside DMM and document the limitation.
  Current implementation note: Decky's `Focusable` supports `onCancelButton`, so the Games tab body intercepts cancel while a game is selected and clears the selected game/mod focus/search. The route shell now also intercepts `GamepadButton.CANCEL` when the Games tab has a selected game, so B should return to the game list even when focus is on controls outside the selected-game body. The existing `Change Game` control uses the same path, and both paths log the source for SSH diagnosis. Package built, installed through the privileged wrapper, rebooted, package parity passed, and `/api/health` returned ok. Needs live Gaming Mode validation because Decky may still own cancel in some outer-route focus states.

△ 55% Add Nexus exploration to the Decky plugin in the correct context.
  Live feedback: Nexus exploration should be available from the selected-game workflow without crowding the primary Decky panel or sending users through a generic external Nexus button.
  Required UX: when a game is selected, show a game-scoped `Browse Nexus Mods` modal. Do not keep a global `Open Nexus Mods` button in the Manage tab, and do not keep a separate external `Open Nexus Page` button beside the in-app browser.
  Current implementation note: selected-game Decky view exposes a game-scoped `Browse Nexus Mods` Decky modal backed by backend REST routes that call Nexus search/files through the Go Nexus adapter, with result cards, sort/search/time-window filtering, file loading, and `Install` actions that reuse the captured-download pipeline. The newest package makes the modal a fixed header/filter layout with one bounded result scroller and explicit card sizing so result rows cannot collapse into slivers in Decky. The global external `Open Nexus Mods` button, the game-level external `Open Nexus Page` bridge, and the duplicate selected-game browse button were removed; Nexus browsing now stays inside DMM except for per-mod page opens in the controlled BrowserView, where the user clicks Nexus Mod Manager Download and DMM captures the generated `nxm://` link. BrowserView close/capture now preserves the selected game context and returns to the Decky Games tab instead of the generic home route. The phone/tablet Browse Nexus panel uses the same sort set and time-window filters. Local Decky fix switched the Nexus modal search control from a raw HTML input to Decky's native `TextField`, removed Search as the preferred focus target, and keeps the current `Vortex Only` toggle as the right single-provider UX until additional browse backends exist. Live backend validation confirms `sort` and `time_window` query parameters are accepted and a named Stardew search returns Vortex-compatible results. Needs live modal keyboard/file-install validation after deployment.

✓ Resolve blocked Stardew install candidate warning.
  Live feedback: Stardew diagnostics still report `1 downloaded archive(s) are blocked by unsupported install planning`.
  Required fix: inspect the blocked candidate, determine whether it is a valid Nexus/Vortex-installable archive we should support, and either implement the missing generic extension/install-plan capability or present a clear user-facing recovery/removal path. Do not leave a vague warning with no action.
  Verified state: live API inspection showed the current Stardew warning was a stale blocked candidate for the same Nexus mod/file tuple as an already installed Generic Mod Config Menu mod, not a missing current installer capability. The backend now removes exact duplicate install candidates when an install completes and before diagnostics/Action Center reads, logs the cleanup, and emits an install-change event. Storage and server tests cover the cleanup. After package deployment and reboot, `/api/games/413150/install-candidates` returned `[]` and diagnostics reported `blocked_candidates: 0`.

△ 84% Expand Vortex-style metadata evaluator to real one-for-one extension-framework parity.
  Required behavior: install planning must be driven by extension-owned rules and Vortex-verified behavior, not one-off mod-specific logic. The evaluator must support installer rules, attributes, dependencies, mod types, choice pauses, target mappings, runtime requirements, load-order/merge declarations, lifecycle hooks, blocked-candidate retry/continue, and safe validation of extension outputs.
  Required boundary: every supported game must have an explicit DMM extension. Core packages may expose reusable primitives, but they must not claim support for a game, Nexus domain, archive layout, launch tool, runtime dependency, Workshop behavior, load-order file, or deployment root unless the game extension declares it.
  Completed locally: F4SE/SKSE dirty-state marker detection moved out of generic Steam discovery and into extension-declared unmanaged marker metadata consumed by the generic discovery/diagnostic path.
  Completed locally: the generic plugin activation format formerly shaped around Fallout 4 is now named `asterisked`, and Fallout/Skyrim extensions select that neutral format explicitly.
  TexMod direction: model TexMod through the existing extension-declared launch-tool capability where possible, with Prototype/Prototype 2 extensions declaring the TexMod executable, `.tpf` payload installer rules, required files, and primary launch behavior. If TexMod needs enabled-mod package lists or generated config, add a typed optional dynamic launch-input spec to the generic launch-tool API, then have the Prototype extensions use it. Static launch tools such as SMAPI should continue to use the simple executable/arguments/required-files path.
  Current state: Stardew has useful extension-owned behavior, but the system is not yet broad enough to claim Vortex extension parity or to build Fallout 4, Final Fantasy VII Rebirth, Witcher 3, and Skyrim without likely rewrites.
  Current implementation note: verified Vortex source checkout at `/tmp/dmm-vortex` commit `d6e16a8`; target extensions show the next parity gaps are Gamebryo merge/plugin/load-order support, Witcher 3 load-order/script-merge/config-merge lifecycle hooks, and Stardew config-merge/runtime lifecycle features. DMM now exposes registered extension capability summaries through `/api/extensions` for audit/debug. Added a generic `archive-root` instruction mode so Vortex-style default `queryModPath` behavior can be expressed declaratively, and used it for initial Fallout 4/Skyrim SE `Data` deployment plus partial Witcher 3 loose mod-root support. Added extension-declared `StripCommonRoot` behavior based on Vortex's `stripCommonRoot` installer helper and the target games' verified `mergeMods: true` declarations, so simple wrapped Data/Mods archives stage to the intended mod path instead of preserving an extra wrapper directory. Added extension-declared stop-folder metadata based on Vortex FOMOD `stopPatterns`/`pluginPath` handling, so Fallout 4 and Skyrim SE FOMOD plans can strip archive wrapper folders before targeting `Data`. Added Go custom installer match/build hooks so procedural Vortex archive transforms live inside the game extension; Witcher 3 now has extension-owned builders for Vortex's `witcher3menumodroot`, `witcher3mixed`, `witcher3tl`, `witcher3content`, and `witcher3dlcmod` installer shapes. Added a reusable Vortex `script-extender-installer` implementation in the extension layer; Fallout 4 and Skyrim SE now declare F4SE/SKSE archive handling with Vortex priority `10`, extension-owned loader names, wrapper stripping up to the loader directory, and game-root target mapping without backend game-specific branches. Added a reusable extension-declared plugin activation capability for Gamebryo-style `plugins.txt`/`loadorder.txt`; generated files are now staged and deployed through DMM's managed deployment plan with a Proton app-data target root, and the same capability now feeds FOMOD plugin dependency state so disabled DMM-managed plugins can match `Inactive` dependencies. Fallout 4 and Skyrim SE now declare Vortex-verified `.ccc` native plugin manifests (`Fallout4.ccc`, `Skyrim.ccc`) so official/native plugin activation state is augmented dynamically from the game root instead of only static lists. Fallout 4 and Skyrim SE no longer advertise placeholder merge/load-order capabilities in `/api/extensions`; they expose plugin activation only until real LOOT/load-order mutation behavior exists. Added extension-declared installer-choice capabilities so FOMOD target roots/mod types come from game extensions; Fallout 4 and Skyrim SE register Vortex-verified `Data` FOMOD roots. Added typed extension lifecycle hook execution for Vortex-style `will-deploy`, `did-deploy`, and `did-purge` events; `will-deploy` can return additional generated-file mappings that still pass through DMM preview/validation/deployment. Added replaceable `will-deploy` mapping results for Vortex `mergeMods`-style path transforms, with local installed-mod IDs attached to deploy mappings so extensions can group files by DMM-owned mod identity. Added a reusable Unreal sortable pak helper verified against Vortex Code Vein/Spyro/Bloodstained pak load-order source; FF7 Rebirth now uses it to rewrite managed pak/ucas/utoc targets under `~mods/AAA-mod-{id}`, `AAB-mod-{id}`, etc. Witcher 3 now implements a concrete extension-owned `will-deploy` hook for the managed subset of Vortex's `mods.settings` behavior: it derives enabled managed mod folder names from DMM target mappings and deploys a generated `mods.settings` file to the game's Proton Documents folder through the validated deployment plan. Added a restore-aware deployment mapping primitive for generated patches to user-owned config files: the extension provides generated target content plus original restore content, the core records both in the deployment manifest, purge restores the original instead of deleting the INI, and purge refuses to overwrite if the user changed the target after DMM deployed it. Fallout 4 and Skyrim SE now register Vortex-verified Gamebryo archive invalidation `will-deploy` hooks that merge only `[Archive] bInvalidateOlderFiles=1` and `sResourceDataDirsFinal=` into `Fallout4.ini`/`Skyrim.ini` under Proton `Documents/My Games`, without backend game-specific branches. FOMOD conditional file installs now support selected-plugin flag dependencies, active/missing/inactive plugin file dependency predicates through injected target-state resolution, and minimum game-version predicates through extension-provided semantic game version state. Skyrim SE and Fallout 4 now register extension-owned semantic game-version providers backed by the standard-library PE VERSIONINFO parser for `SkyrimSE.exe` and `Fallout4.exe`; Steam build IDs remain diagnostics only. The extension SDK now supports Vortex-style ignored conflict patterns, and Fallout 4 registers the verified persistent subgraph offset pattern; the deploy planner consumes those patterns generically and skips matching unmanaged targets instead of blocking or overwriting them. Added extension-declared default deployment strategies and review-state deployment policy; Fallout 4, Skyrim SE, Witcher 3, and FF7 Rebirth now opt into review-state deployment for the actively supported test targets, while FF7 Rebirth declares copy as its default strategy to match the verified no-symlink extension guidance. Added a reusable extension-layer BepInEx helper that mirrors Vortex's shared `modtype-bepinex` installer concepts for injector/runtime archives, root mods, plugins, ConfigurationManager archives, FOMOD exclusion, wrapper stripping, and runtime presence checks; Citizen Sleeper, Hollow Knight, and Disco Elysium now consume this helper instead of carrying duplicate per-game BepInEx installer code. The evaluator still needs advanced Witcher menu XML merge/manual load-order semantics, more concrete per-game version providers where Vortex declares them, LOOT/load-order sorting, Stardew hook cleanup, and live Fallout/Skyrim/Witcher/FF7 validation before this item can be considered close.
  Current Stardew config note: the extension now mirrors the Vortex config-mod safety goal by adopting generated SMAPI `config.json` files into a profile-owned generated staging area, refreshing them before disable, and redeploying them on re-enable through a new checksum-only `adopt-existing` deployment policy. Unit tests pass. Live Deck validation passed by adopting two generated Stardew config files into the deployment manifest, disabling Generic Mod Config Menu, confirming its game-folder config was removed while the saved generated copy remained, re-enabling the mod, confirming the config returned, and rerunning the Stardew MVP live acceptance check cleanly with 92 deployed files.

△ 96% Solidify the extension architecture before building more game extensions.
  Decision summary: DMM extensions are first-party managed Go extension modules built from Go structs and registration code. JSON/YAML/Lua/Starlark must not be the behavior source of truth for MVP.
  Source of truth: the Go extension object/registration code owns extension behavior and metadata. The database may store a runtime snapshot of registered extension identity, capabilities, and version for UI/debug/audit purposes, but it must not become a second behavior source of truth. A separate JSON metadata file is not needed for first-party compiled MVP extensions unless runtime-loaded external bundles later require a small discovery manifest.
  Required SDK shape: define a small Go extension SDK with stable registration interfaces such as `RegisterGame`, `RegisterInstaller`, `RegisterModType`, `RegisterRuntimeRequirement`, `RegisterLaunchTool`, `RegisterMerge`, `RegisterLoadOrder`, and `RegisterEventHandler`. Avoid leaking internal storage/server packages into the SDK.
  Required first implementation: migrate Stardew Valley onto the intended SDK boundary first and keep game-specific behavior inside the Stardew extension package. Do not build more non-Stardew game extensions until this is stable enough that each extension is written once against the intended API.
  Core/backend owns: config, persistent storage, Nexus/download jobs, archive safety and extraction, staging layout, profiles, profile state, deployment manifests, conflict detection, apply/purge/rollback execution, WebSocket domain events, HTTP APIs, Decky/Steam privileged action requests, logging, and final filesystem writes.
  Game extensions own: game identity, Steam/store/Nexus identifiers, detection rules, supported runtime platforms, deployment eligibility, mod types, target roots, tool definitions, primary launch-tool requirements, installer classification, install-plan generation, metadata extraction, runtime/dependency requirements, load-order semantics, merge/generation algorithms, game-specific prepare/repair actions, collection/export hooks, and lifecycle hooks.
  Required validation: every install plan, generated file, target mapping, merge output, and runtime action returned by an extension must be validated by the core for path traversal, unmanaged overwrite risk, declared mod type/root, profile ownership, and deployment manifest consistency before files are written.
  Current implementation note: removed the separate Stardew `InstallPlanSpec()` helper and converted first-party game extensions to return `internal/extensions/sdk.Extension` specs. The core now compiles SDK specs into internal `gameext.Extension` records before registry construction, so Stardew, Fallout 4, Skyrim SE, and Witcher 3 import the SDK boundary rather than constructing internal registry state directly. The extension registry stores compiled extension modules and returns stable summaries of registered games, Nexus domains, sources, installers, installer choices, mod types, runtime requirements, launch tools, game-version providers, plugin activations, merges, load orders, and event handlers through `/api/extensions`. Backend startup now persists a non-behavioral extension capability snapshot to SQLite and exposes it through `/api/extensions/snapshots` for debug/audit, while live Go registrations remain the behavior source of truth. Extension specs now require explicit `Version` and `BuildID` values, and snapshot/API responses expose them for audit/debug. Installer specs now support extension-owned custom match/build hooks for procedural Vortex installers, with the core still validating and staging returned instructions. Event specs now support typed Go lifecycle handlers; pre-deploy hooks may return generated deployment mappings that the core validates and applies, while post-deploy/purge hooks observe completed state without bypassing the manifest/rollback path. Post-deploy/purge extension messages now become deduped `extension-notice` jobs so extension-required manual steps appear in Action Center and Decky notifications instead of logs only; Ghost Recon Breakpoint uses this for its source-verified AnvilToolkit repack reminder. Game-version provider specs now let extensions supply Vortex-style semantic installed game versions while the core stores Steam `buildid` separately for diagnostics/provider input. Hook input now includes the Steam library path so extensions can derive Proton/compatdata external target roots without importing storage or hardcoding that logic in the core. Tests cover the registry compiler, Stardew/Fallout/Skyrim/Witcher endpoint output, install planning, custom installer hooks, runtime requirements, FOMOD installer-choice roots, multi-root deploy planning, Gamebryo plugin activation generation, extension lifecycle hook execution, extension notice jobs, extension version/build IDs, game-version provider registration, and extension snapshot persistence. Still incomplete: runtime-loaded bundle packaging, SDK package hardening, advanced concrete game hook implementations, generic extension tool-runtime actions from notices, concrete game-version providers, and non-Stardew live validation.

△ 96% Add meaningful MVP rollback UX/UI.
  Current state: apply-time rollback safety exists in the deployment path, but the user does not see rollback in a useful product form.
  Required UX: ordinary users should see a clear restore/recovery action only when rollback matters. The current single recovery button is not sufficient for MVP because it does not explain the restore point, time period, affected files, profile/game context, or expected delta. Power users must be able to inspect restore points, timestamps, deployment source, profile name, changed/removed/restored file counts, file-level deltas, rollback jobs/events, and what files were restored from an advanced/activity view. Users must be able to choose a specific DMM deployment point to restore to, preview the delta before applying it, and understand that recovery only touches DMM-owned files tracked by deployment manifests. Do not overexpose rollback internals in the default mod workflow.
  Current implementation note: `/api/games/{appID}/deploy/status` now reports automatic failed-apply rollback protection, restore/repair/purge availability, a recovery summary, and a restore summary. `POST /api/games/{appID}/deploy/restore` creates a rollback job and restores only DMM-owned files recorded in the active deployment manifest to the last applied source state. `GET /api/games/{appID}/deploy/history` exposes recent deployment records from the existing manifest tables, and the phone/tablet Advanced Deployment Tools panel now shows `Restore Last Applied State` behind confirmation plus a compact deployment-history list for power users. The primary Mods page also shows a simple Restore banner when rollback is available, so ordinary users can recover the last DMM-applied state without reading file-operation details. The Decky selected-game route now reads deployment status through the plugin bridge and shows a compact Recovery row with confirmation before calling the restore endpoint. Focused backend tests prove a changed DMM-owned copied file is restored through the route and history returns recent deployment summaries, the web build passes, the live Deck backend returned completed `rollback` jobs with no issues, live history returned recent Stardew deployments, and `testing/live_rollback_check.sh` now verifies the public restore endpoint against the active Stardew deployment before rerunning the MVP live acceptance check. Still needs live UI review, rollback/failure toast coverage, and future arbitrary restore-point/rollback-preview UX before completion.

△ 96% Support coexistence with Steam Workshop mods.
  Required MVP behavior: Steam Workshop content must not automatically make a game undeployable. DMM should detect workshop content for the selected Steam app, label it as externally managed Workshop content, leave it untouched, and still allow DMM-managed Nexus deployments alongside it when the game extension declares this is safe.
  Required safety: deployment conflict detection must still block if a DMM mapping would overwrite a real file in the game folder or appdata. Workshop directories under Steam `workshop/content/{appID}` or app-specific workshop metadata should be treated as external state for diagnostics, not as DMM-owned files.
  Required UX: the Review tab should distinguish "Steam Workshop content detected; DMM will leave it alone" from risky unmanaged files in the game directory. This should be one of the next three implementation items after the current package/deploy cycle.
  Current implementation note: Steam discovery now detects `steamapps/workshop/content/{appID}` and `steamapps/workshop/appworkshop_{appID}.acf` as `steam_workshop` diagnostics. The extension SDK/registry now has first-class Steam Workshop policy and action declarations. Current first-party targets declare coexistence safe and now declare standard first-party Workshop actions (`subscribe`, `unsubscribe`, `enable`, `disable`), so DMM labels Workshop as Steam-owned external state and can queue management intent for supported game extensions. Unsupported games with Workshop content are still marked for review. Project Zomboid and Stellaris are now registered as Workshop-only first-party extensions with no Nexus domains or installer rules, and the SDK validator allows capability-specific Workshop-only extensions instead of requiring fake Nexus domains. Live API validation after deployment showed Project Zomboid, Kenshi, RimWorld, and Stellaris as Workshop-supported when installed. `/api/games/{appID}/workshop` exposes the supported action contract live. The backend now returns non-persisted placeholder Workshop rows from detected Steam item IDs before Decky syncs richer SteamClient item details, and the UI disables enable/disable for placeholder rows while leaving them visibly Steam-managed. Decky startup sync now verifies Steam's `GetSubscribedWorkshopItemDetails(appID, publishedFileIDs)` shape from Steam UI source and live runtime state; Kenshi's 15 Workshop items now return real titles through the API after startup sync. Storage preserves previously known titles and disabled-state values when a later SteamClient read omits those fields, while still replacing observed membership/order. No-op Workshop syncs now return `changed=false` and do not emit `workshop.changed`, preventing Decky selected-game refresh loops and reducing sidebar lag. Latest post-reboot live validation reports 15 Kenshi Workshop rows, 15 titled rows, and known disabled state for the first visible rows. A safe unchanged Kenshi Workshop order job completed through the backend-owned/Decky-executed `steam-workshop-action` path with `steam_workshop` source tagging. Live reversible validation disabled Kenshi Workshop item `1358096888` (`Reactive World`) through job `job-243`, observed `disabled_locally=true`, then re-enabled it through job `job-244` and observed `disabled_locally=false`. Remaining live gap: unsubscribe still needs explicit user-approved validation because it changes Steam subscription state.
  Steam API management scope: subscribing, unsubscribing, enabling, disabling, load ordering, or profile-controlling Steam Workshop mods uses the same backend-published/Decky-executed action boundary as launch-option changes. Verified local `@decky/ui` SteamClient types expose `SubscribeWorkshopItem(appId, workshopId, subscribed)`, `SetWorkshopItemsDisabledLocally(appId, itemIds, value)`, `SetWorkshopItemsLoadOrder(appId, itemIds)`, `GetSubscribedWorkshopItems(appId)`, `GetDownloadedWorkshopItems(appId)`, and `DownloadWorkshopItem(appId, itemId, highPriority)`. Backend now persists a mirrored Workshop item view from Decky sync, exposes `/api/games/{appID}/workshop`, queues `steam-workshop-action` jobs, atomically claims actions before Decky execution, and records completion/failure through the backend. Decky now syncs Workshop items from SteamClient, executes queued enable/disable/subscribe/unsubscribe actions, and exposes visible Workshop item controls inside the selected game's Decky Mods pane. Failed Workshop actions now have an explicit retry endpoint that moves the same job back to `waiting` for Decky execution, and failed Workshop jobs can be canceled from Action Center so temporary Steam API failures do not leave stale actions stuck in the user's flow. `go test ./...`, focused storage/server tests, Python syntax check, web build, Decky build, package build, and install-wrapper deployment pass. Needs post-reboot live validation of the new Decky UI and a real Workshop-enabled game to verify Steam mutation behavior before completion.

△ 55% Verify toast coverage after the event architecture lands.
  Current state: download/capture toast worked in at least one live test, but do not mark complete until capture, download, install, FOMOD-choice-required, deploy success, deploy failure/rollback, and launch-tool failure toasts are checked against current code.
  Current implementation note: Decky notification handling now listens to `captured-install`, `installer-choice`, `deploy`, `purge`, `repair`, `recover-downloads`, and `rollback` jobs through one generic job-toast path. Deployment/rollback/purge/repair failures now get action-specific failure titles, and job toasts are seeded from the backend job snapshot plus live WebSocket events. Live logs initially showed old deploy events replaying as toasts after reboot; fixed by requiring recent timestamps for WebSocket replay/snapshot toast paths while allowing direct Decky user actions to toast immediately. Latest package narrows status policy so only captured downloads toast queued/running states, installer choices/extension notices toast waiting states, and deploy/rollback/purge/repair/Workshop actions toast only completed or failed states. This removes duplicate "deploy running" overlays while preserving the notifications users need. Decky and web builds pass, package deployed, backend health verified. Needs live confirmation that capture/download, FOMOD-required, deploy success/failure, rollback, and launch-tool failure toasts appear in Gaming Mode after the toast policy package is installed.

△ 78% Code-review and remove deprecated paths.
  Required cleanup: remove old approval wording, legacy request/install naming, fallback launch-option mutation paths, stale tests, stale docs, old polling branches, stale local-only preference paths, and any compatibility code for obsolete MVP shapes. Breaking changes are preferred over preserving dead paths.
  Current implementation note: removed stale user-facing "download approval" language from backend messages, web Action Center/settings, Decky settings, and wording-sensitive tests. The captured install API is now the breaking `/api/captured-installs` route family with job type `captured-install`; the old `/api/imports/pending` route and `pending-import` job type were removed with no compatibility alias. Internal server/storage names and tests now use captured-install terminology, including `captured_installs` storage and reset counters. Web, Decky, and the nxm handler call the new route. Removed the startup obsolete-job cleanup shim for retired `pending-import`/`launch-config`/old deploy wording instead of preserving a legacy compatibility path. The manual URL resolver is now the breaking `POST /api/captured-installs/resolve` route with `captured-install-resolve` job wording; `/api/imports/resolve` has no compatibility alias. Removed the old bare-array staged-manifest parser and updated fixtures to the current manifest envelope shape with `files`, `mod_type`, and plan metadata. Removed the hidden effective-staging-path fallback that silently preferred a reconstructed current data-dir path over the installed mod's recorded staging path; missing/stale staging paths now surface as needs-recovery so the current model can be fixed directly. Latest grep pass found no old route aliases, no local-only game preference stores, and no old global `Open Nexus Mods`/`Open Nexus Page` Decky controls. The remaining `plugin` terms are Decky packaging or Bethesda plugin activation/load-order terminology, while `request` hits are HTTP/internal action plumbing. The live profile-toggle verifier was updated away from the retired separate-apply workflow and now checks the current one-click enable/disable/apply behavior through public API state. README and testing docs now describe captured installs/actions instead of local-install approval requests. Internal FOMOD selection variable names and captured-install log fields now use submitted/capture language instead of request-source wording. Archive-helper dependency errors now tell the user to retry the install, and captured-install retry jobs now say "Retry started" instead of "Retry requested". `go test ./...` and `testing/ui_mvp_audit.sh` pass. Remaining cleanup: old local variable names that are not user-facing but still read like the pre-Action-Center model, and a final grep/review pass before MVP approval.

△ 45% Planning-doc cleanup.
  Required cleanup: align `TODO.md`, `ROADMAP.md`, `docs/extensionFramework.md`, `extensionTargets.md`, and user-facing docs with the actual current architecture and UX language. Remove claims that scaffolding equals completion.
  Current implementation note: `TODO.md` no longer says feature work is paused on the old Decky navigation branch or that the Fallout false-conflict fix is uncommitted. `TODO.md` now lists the current remaining MVP queue, including update-check validation and the latest Nexus/FOMOD/rollback/Decky validation state. `ROADMAP.md` now uses captured-install terminology for the main UX and fixes stale "install request" and `defaultValue` wording. Remaining cleanup: README/testing docs still contain some internal staging/history references that need review for whether they are user-facing or intentionally technical; extension framework docs and extension targets need a final pass after the next extension-parity push.

△ 97% Manage mod updates.
  Required MVP behavior: users can check installed mods for updates, clearly see current/available/unknown/unsupported/error state, install an available update through the same safe captured-install pipeline, and keep update activity visible in Action Center/jobs/toasts without losing profile context. Update installation must preserve the normal safety model: source identity stays attached, FOMOD/installer choices still pause when required, the updated mod installs disabled unless the user explicitly enables or has enabled auto-enable, profile targeting is explicit, and file deployment/rollback still flows through DMM-owned manifests.
  Required provider boundary: update checks and update installs must be source-specific. Nexus, GitHub Releases, Modrinth, Thunderstore, GameBanana, mod.io, and CurseForge use verified resolver/update capabilities where available. Direct, local archive, Steam Workshop, and unsupported providers must show explicit unsupported state rather than disappearing or pretending updates are available.
  Required UX: phone/tablet and Decky mod rows should expose update state without crowding the normal enable/disable controls. Simple users should see "Update available" and a safe install action; power users can inspect provider/file/source details in advanced/job views.
  Current implementation note: DMM now stores Nexus update-check observations per installed mod, exposes `POST /api/games/{appID}/mods/check-updates`, includes cached update status on `/api/games/{appID}/mods`, and emits `mod_updates.changed` so web/Decky mod panes refresh. The checker asks Nexus for the installed mod's file list, compares the installed file against the newest file in the same Nexus category when possible, and reports `current`, `available`, `unknown`, `unsupported`, or `error` without changing the installed mod or deployment state. The phone/tablet Mods panel now has a `Check Updates` action, per-row update status, and `Install Update` for available updates. The Decky Games route exposes the same check action and uses Y to install an available update from a focused mod row while preserving A for enable/disable, Options for remove, and Menu for reinstall. Backend update install uses `POST /api/games/{appID}/mods/{installedModID}/update` and turns the update into the same `captured-install` pipeline used by Nexus captures, so existing download queue, FOMOD choices, auto-install, auto-enable, Action Center, and browser-required non-premium failure behavior remain the single product path. `go test ./...`, web build, Decky build, Python Decky bridge syntax check, package smoke, Deck install-wrapper deployment, package parity check, backend health check, and live Stardew update-check API validation passed on the current package for eight installed mods across Nexus and GitHub. New focused tests cover queued update jobs and browser-required failures. Still needs Gaming Mode Decky visual review and a real available-update validation when a test mod has a newer provider file.

Right After MVP / Post-MVP Work Allowed Before The Stop Marker

△ 45% Windows/Proton Stardew support through extension metadata.
  Current implementation note: the extension SDK now supports extension-declared install platforms, platform-specific installer variants, and platform-specific launch-tool variants. Stardew declares Windows/Proton via `Stardew Valley.exe` and native Linux via `StardewValley`; the SMAPI installer now selects the Vortex-verified `internal/windows/install.dat` or `internal/linux/install.dat` payload through metadata, and the Steam launch action targets `StardewModdingAPI.exe` for Windows/Proton installs. Focused tests cover Windows SMAPI payload planning and Windows launch-tool action generation. Still missing: live Proton Stardew validation, Windows SMAPI download/install/deploy test, Windows launch option application through Decky, and any Proton-specific file/path edge cases discovered on real Deck storage.
△ 42% Broader runtime requirements contract: loaders, script extenders, launch tools, game tools.
  Current implementation note: launch tools now support platform-specific executable/required-file variants selected by extension-owned install-platform markers. Launch tools also support extension-declared command-line arguments with platform-variant overrides, and the Steam launch-option action now builds desired options from executable plus arguments without game-specific backend code. This was source-verified against the public Vortex Star Wars Battlefront II extension, whose Frosty primary tool declares `-launch default`; DMM's Battlefront II extension now carries that parameter through the generic SDK/API path and `/api/extensions` exposes launch executable/arguments for audit. Runtime dependency diagnostics now evaluate extension-extracted SMAPI manifest dependency minimum versions through a Vortex/Stardew-verified semver-coercion path, report enabled-but-outdated dependencies separately from missing dependencies, and choose the highest enabled installed version when duplicate logical IDs exist. Added a reusable extension-layer script-extender runtime requirement helper; Fallout 4 and Skyrim SE now register F4SE/SKSE file-presence diagnostics from extension metadata instead of core game-specific checks. Decky now loads selected-game diagnostics and displays missing/outdated runtime requirements in the game view, with launch-tool issues wired to the existing Decky/Steam launch setup action. Still missing: richer tool categories, optional/manual tools, dependency install helpers, broader version constraint syntax, deeper tool health checks, and polished Decky/UI flows for installing/configuring non-primary tools.
△ 68% Conflict resolution with per-file winners or load-rule semantics.
  Current implementation note: active duplicate-target deployment previews now carry loser and winner installed-mod IDs, source mod IDs, profile priority, full target paths, and winner reason into skip actions. DMM now stores profile-scoped per-file winners in SQLite, validates winner choices against the active duplicate-target preview before persisting them, feeds those overrides back into the deploy planner, and applies the profile immediately after a winner changes. The phone/tablet Advanced Deployment Tools panel now shows "File Winners" cards for duplicate managed targets, lists the candidate mods with source pills, lets power users choose a specific file winner, and lets them reset that file back to profile-order resolution. The Decky full DMM Mods route now loads deploy preview state and shows compact source-tagged "File Winners" rows where `A` chooses the next candidate and `Y` resets to profile order. Focused planner/storage/server tests, `go test ./...`, web build, Decky build, package build/deploy, installed-package parity, backend health, and live Stardew deploy-preview smoke checks passed. Still missing: live UI validation with a real conflicting mod pair, LOOT/load-order integration where relevant, and richer extension-owned load-rule semantics.
△ 86% Download queue controls: concurrency, throttling, partial resume, richer retries.
  Current implementation note: captured Nexus downloads now enter a queued state first and use a dynamic backend download-slot gate before transitioning to running. Queued downloads can be canceled before any network fetch starts, and restored queued/running captured-install jobs still recover as waiting retry actions after backend restart. Backend config now persists `download.max_concurrent_captured_downloads` and `download.max_concurrent_captured_downloads_per_game`, exposes active/max slot state plus active `catalog:gameDomain` groups through `/api/status`, and updates the live gate through patch-style `PUT /api/settings/downloads` without restarting. Decky Settings exposes compact cycle controls for global and per-game captured downloads; phone/tablet Install settings exposes both backend values through selects. The keyed gate defaults to one active captured archive download per provider/game domain, so duplicate Nexus downloads for the same game queue behind each other even when the global limit allows different games to download in parallel. The download worker retries transient failures on each Nexus-provided link before moving to the next mirror, using typed download status errors so 408/429/5xx and transport failures retry while permanent 4xx and validation errors fail fast. HTTP `Retry-After` headers are parsed and used for retry delay with an MVP clamp so rate-limit responses do not hold the queue indefinitely. Captured installs now persist a Nexus file-list-derived archive filename when available, and the downloader can resume `.part` files with HTTP Range requests for captured archives while restarting cleanly when a server ignores Range. DMM logs every failed attempt and retains the captured install for user retry only if every automatic attempt is exhausted. Focused tests cover persistence, queued cancellation, keyed same-game throttling, transient retry success, mirror retry success, exhausted-retry retention, permanent failure handling, `Retry-After` parsing/backoff, resumable 206 downloads, ignored-Range restart, captured filename persistence, `go test ./...`, Python bridge syntax, web build, Decky build, package smoke, package deploy, package parity, and live API update/reset all passed. Still missing: live visual review of the Decky/web controls and live interruption/resume validation against a real Nexus archive.
△ 25% Collections/bulk install.
  Current implementation note: DMM now supports MVP-safe bulk URL capture by accepting multiple provider URLs and routing each URL through the same `/api/captured-installs` pipeline used for single Nexus/Thunderstore/GitHub/Modrinth/GameBanana/mod.io/CurseForge/direct captures. Phone/tablet Add Mod accepts one link per line or comma-separated links, preserves the selected install profile, reports accepted/failed/browser-required counts, reuses duplicate active captures, and keeps failed URLs in the text box for correction. Backend tests cover source/profile preservation and duplicate collapse. This intentionally does not claim full Vortex/Nexus Collections parity yet: Vortex collections include manifest rules, bundled files, installer choices, binary patches, and game-specific parser hooks, all of which still need source-verified design before DMM can import real collection manifests safely.
△ 82% Saved FOMOD choice presets for unattended reinstalls.
  Current implementation note: DMM now persists exact-file installer-choice presets after a FOMOD candidate successfully stages, keyed by game/catalog/mod/file/installer kind. Future choice sessions for the same exact file reuse the saved selections as their initial state instead of recomputing static defaults, and captured installs now use an exact-file preset to stage the FOMOD directly without creating an Action Center installer-choice request. The MVP version-crossing policy is intentionally exact-file only: DMM does not silently reuse saved installer choices across different Nexus file IDs, and the preset list API/UI now exposes `reuse_scope: exact_file` / "Exact file only" so that policy is visible. The direct preset path still uses the shared validated FOMOD build/staging helper, saves the preset again after success, and names unattended FOMOD installs from the module metadata when available. The same shared captured-install staging path is used by cached installed-mod reinstalls, and focused tests now prove reinstalling an existing FOMOD from cache reuses the exact-file preset without creating installer-choice jobs. The backend exposes per-game preset list/delete routes, always returns `[]` for empty lists, logs preset deletion, and emits install-change events. The phone/tablet game Action Center now shows saved exact-file installer choices and lets users forget a preset so the installer asks again. Phone/tablet FOMOD cards and Decky FOMOD modals now call out when choices are preselected from DMM's saved/default installer state, without claiming every preselection came from a saved preset. Tests cover successful preset persistence, exact-file reuse, unattended exact-file preset staging, cached reinstall preset reuse, empty/list/delete API behavior, focused FOMOD captured-install behavior, preset reuse scope output, `go test ./...`, web build, Decky build, package smoke, Deck package deploy, package parity, and live Stardew empty preset-list/health validation passed. Still missing: live FOMOD reinstall validation.
△ 90% Deployment strategy selection per game/profile/filesystem.
  Current implementation note: DMM now stores selected-profile deployment strategy overrides in SQLite profile state and resolves effective strategy as profile override, game default override, then extension default. `GET/PUT /api/games/{appID}/deploy/settings` edits the selected/default profile by default and returns profile/game/extension layers explicitly. The phone/tablet Advanced Deployment Tools panel now labels the strategy as a profile override, sends the selected profile ID, and shows the effective source plus game and extension defaults. The backend still supports explicit game-scope overrides as an advanced default layer. The backend reports non-invasive filesystem capability recommendations per game: symlinks on SteamOS/Linux, hardlinks only when DMM data and game folders are on the same filesystem, and copy when the game path exists. Deployment status now reports the manifest-level deployment strategy instead of accidentally sampling a copied runtime file, so mixed deployments such as symlinked profile mods plus copied launch/runtime files remain understandable. Focused backend/storage tests cover profile override/reset, game default override/reset, preview/apply strategy resolution, invalid values, capability reporting, and manifest-level status strategy; `go test ./...`, web build, Decky build, package deployment, backend health, live Stardew profile-strategy override/reset API validation, preview-strategy validation, and MVP live acceptance passed. Still missing: live visual validation of the updated Advanced panel and deeper probe-mode warnings if a specific target directory rejects symlink/hardlink creation despite filesystem heuristics.
△ 45% Load order/plugin sorting, likely LOOT or handler-specific.
  Current implementation note: DMM now has a normalized profile mod order endpoint (`PUT /api/profiles/{profileID}/mods/order`) that validates all mod IDs belong to the profile's game, rewrites priorities in one transaction, emits profile events, and applies the selected profile. The phone/tablet Advanced mod controls now move mods up/down through this order endpoint instead of mutating arbitrary priority integers. The Decky selected-game mods pane now exposes an explicit Order Mode that forces profile-order sorting and uses focused-row controller actions to move mods up/down without adding cramped horizontal buttons. This order already feeds the existing deployment planner and Gamebryo plugin activation generation. DMM now exposes a read-only `/api/games/{appID}/load-order` endpoint for extension-owned plugin activation, reporting native plugins plus enabled DMM-managed plugin files with installed-mod IDs and priorities. The phone/tablet Advanced Deployment Tools panel shows that plugin activation/load-order preview when supported, and the Decky selected-game Mods route shows a compact first-eight plugin preview. The deploy planner and load-order preview now share the same active-mod mapping helper. Live Deck API validation after package deploy returned Fallout 4 native plugin activation rows and Stardew unsupported/empty load-order state. Still missing: mutation controls for plugin rows, LOOT/masterlist/userlist support, extension-specific sorters beyond current hooks, Decky live visual validation, and live Fallout/Skyrim mod deployment validation.
△ 90% Nexus browsing/search inside DMM using the configured Nexus API key.
  Current implementation note: backend route `GET /api/games/{appID}/nexus/mods` derives the extension-owned Nexus domain from the selected Steam app and calls the Nexus adapter. Decky now opens a wide Nexus browser modal from a selected game, searches/browses Vortex-compatible results by default, supports A-Z/downloads/unique downloads/popular/updated/relevance sorting, and applies a server-side updated-after window. The primary install handoff is no longer a direct `Show Files` / API-download path: each result opens the real Nexus mod page in DMM's controlled BrowserView, and the user clicks Nexus' own Mod Manager Download / Vortex link so DMM receives the short-lived browser-generated `nxm://` credential. The modal owns its own bounded scroll region, keeps filters and the close action at the top, uses Decky's native `TextField` for controller-focusable search input, and logs Nexus page handoffs through the Decky plugin log. After a successful captured download starts, the BrowserView closes and returns to the selected game's DMM page. Nexus GraphQL date filters are sent as `YYYY-MM-DD` because the live Nexus backend rejects RFC3339 timestamps in the generated Lucene query. Empty Nexus result sets now return `mods: []`, not `mods: null`. The phone/tablet game workspace exposes a compact `Browse Nexus` disclosure inside `Add From Nexus`, searches Vortex-compatible results by default, and opens results on the Deck for the same browser-generated capture flow. Both Decky and phone/tablet Browse Nexus surfaces expose an explicit advanced `Vortex Only` / `All Mods` compatibility filter while keeping Vortex-compatible results as the default safe path; unsupported archives still block through the normal installer planner. Live backend validation on the Deck returned Stardew all-time Nexus search results and verified empty time-window searches have the correct array response shape. Live non-premium validation showed direct HTTPS file installs cannot fetch Nexus API download links without a browser-generated `nxm://` key; the runtime now treats that as browser-required instead of a green success. Still needs Decky modal keyboard validation, real `nxm://` FOMOD validation, and phone/tablet UI validation.
  Latest deployed state: browser-required Nexus update and pasted HTTPS paths now return completed handoff jobs with `browser_required: true` instead of failed captured-install jobs. Package `9a6dca8` was installed on the Deck, package parity passed, `/api/health` returned OK, live web assets were reachable, and stale pre-fix failed probe jobs were cleared through the normal captured-install cleanup API.

△ 15% Final MVP phone/tablet UX overhaul.
  Required behavior: the phone/tablet app must feel like a polished first-class product before MVP approval, not a backend debug UI. Redesign around clear game context, profile-first mod management, Action Center, installer-choice/FOMOD flows, source pills, update state, rollback/recovery, and advanced deployment tools that do not overwhelm normal users.
  Tablet/iPad requirement: tablet layouts must be first-class and use the available space intentionally rather than stretching the phone layout.
  Priority: this is the final MVP gate after backend/deployment/FOMOD/provider behavior stabilizes, so the redesign is not churned by moving workflow targets.
  Current implementation note: latest web polish turns the top status strip into actionable navigation for Action Center, Manage Ready games, and Sources; keeps the app landing on Action Center; and makes blocked malformed installer candidates review-only in the game Action Center instead of showing install-profile/choice controls. Live Deck web assets and `testing/ui_mvp_audit.sh` pass. This is only incremental polish, not the full final visual/interaction overhaul.

△ 80% Build next game extensions from `extensionTargets.md`: Skyrim SE, Fallout 4, Witcher 3, Final Fantasy VII Rebirth, Final Fantasy X/X-2 HD Remaster, Project Zomboid, RimWorld, Kenshi, Halo MCC, Spyro Reignited Trilogy, Marvel's Spider-Man: Miles Morales, Prototype/2, MGSV, X4, Stellaris, Dwarf Fortress, Tabletop Simulator. Verify Vortex extension/source behavior first for each target and add missing extension-framework APIs before encoding game-specific behavior.
  Latest local note: Witcher 3 now registers a source-verified `did-deploy` Script Merger reminder from the extension layer when enabled DMM-managed Witcher mod files change. Tool execution from that notice still belongs to a future generic extension tool-runtime action, not Witcher-specific backend code.
  Current implementation note: Fallout 4 is registered with Vortex-verified Steam/Nexus IDs, `Data` mod-path installer rules, extension-owned F4SE script-extender archive inference, F4SE primary launch-tool metadata, F4SE runtime file diagnostics, Vortex-declared FO4Edit/Wrye Bash/BodySlide tool metadata, and reusable Gamebryo `plugins.txt`/`loadorder.txt` activation generation without claiming unimplemented merge/load-order capability summaries. Skyrim SE is registered with Vortex-verified Steam/Nexus IDs, `Data` mod-path installer rules, extension-owned SKSE script-extender archive inference, SKSE primary launch-tool metadata, SKSE runtime file diagnostics, Vortex-declared SSEEdit/Wrye Bash/FNIS/BodySlide/Creation Kit tool metadata, and reusable Gamebryo plugin activation generation without claiming unimplemented merge/load-order capability summaries. Fallout/Skyrim are not live-tested yet and do not yet implement Bethesda plugin sorting/LOOT, full load-order rules, or live plugin dependency validation. Witcher 3 is registered with verified Steam/Nexus IDs and extension-owned procedural builders for Vortex's menu mod root, mixed mod/DLC, top-level mod, content-only, and DLC installer shapes, while Script Merger archives are blocked with a clear reason. Witcher 3 now has an implemented `will-deploy` hook for a basic managed `mods.settings` file in Proton Documents, but Vortex source verification still shows advanced menu XML merge, script-merger setup/prompts, manual load-order preservation, collection/profile data support, and full load-order UI semantics are incomplete. Final Fantasy VII Rebirth is now registered from the verified cached Vortex extension package v0.4.0, Nexus extension page, and Proton executable layout evidence with Steam AppID `2909400`, Nexus domain `finalfantasy7rebirth`, copy-only default deployment, UE5 sortable pak/ucas/utoc install choices, FF7RML loader and `.uplugin` mod installers, UE4SS root/script/DLL/LogicMods/combo installers, Proton Documents config/save target roots, binary-adjacent files, and root-folder archives. FF7 pak deployments use a Vortex-style sortable Unreal pak folder prefix hook, and DMM now passes original archive filenames into extension planners so FF7 UE4SS script/DLL installers can mirror Vortex's archive-name-derived mod folders. FF7 Rebirth still needs live archive validation across these variants. Kenshi is registered from verified Vortex source with Steam/Nexus IDs, `.mod` archive planning rooted at the folder containing the `.mod` file, deployment under `mods/<modName>/...`, Vortex-declared tool metadata, semantic version discovery from `currentVersion.txt`, and Steam Workshop coexistence/actions. RimWorld is registered from verified Vortex source with Steam/Nexus IDs, one-`About.xml` archive planning, package-ID-derived mod folder naming under `Mods`, filtered deployable files, semantic version discovery from Vortex's `version.txt` path with Steam Deck case-insensitive lookup for the live `Version.txt` file, and Steam Workshop coexistence/actions; live Deck validation reports RimWorld version `1.6.4871 rev598` without the previous lowercase lookup warning. X4 is registered from verified Vortex source with Steam/Nexus IDs, the game-root `extensions` installer, `content.xml` metadata extraction/output-root handling, Steam Workshop coexistence/actions, and a new extension-provided target-root provider for the dynamic Proton Documents path `Documents/Egosoft/X4/{steamUserId}/extensions`; `/api/extensions` on the live Deck includes `x4foundations`. Halo MCC is now registered from verified Vortex source with Steam/Nexus IDs, plug-and-play `modinfo.json`, `modpack_config.cfg`, and recognized Halo game-folder installers, Vortex-declared Assembly tool metadata, `build_tag.txt` semantic version discovery, and an extension-owned Proton `ModManifest.txt` deploy hook that writes enabled DMM plug-and-play staging paths through the normal restore-aware deployment manifest. Spyro Reignited Trilogy is registered from verified Vortex source with Steam/Nexus IDs, `.pak` archive planning rooted at the folder containing the first `.pak`, deployment under `falcon/content/paks/~mods`, FOMOD archive exclusion, and the existing Unreal pak load-order prefix hook matching Vortex's load-order prefix model. Marvel's Spider-Man: Miles Morales is registered from the verified Vortex extension package file `1831` with Steam/Nexus IDs, MMPC `.mmpcmod` archive planning, multi-file installer choices, generated Vortex-style `SMPCTool/ModManager/ModManager.txt` load-order mappings, required game-file diagnostics, and explicit blocks for `.mmpcmodpack` and MMPC tool archives until DMM has generic Vortex submodule and Proton/Windows tool-execution capabilities. Star Wars Jedi: Survivor now mirrors the verified Vortex multi-PAK installer prompt by using a multi-select generic installer choice, preserving selected PAK variants' original relative destinations, and keeping the sortable Unreal pak prefix hook extension-owned. Prototype and Prototype 2 now register verified Nexus domains, live executable/RCF diagnostics, and clear research blockers because no Vortex extension exists in checked sources and observed Nexus mods mix root ASI, TexMod, RCF extraction, and patcher flows. MGSV is registered with the verified Nexus domain and live Steam Deck file checks; SnakeBite `.MGSV` metadata is detected and blocked with a clear reason because source review confirmed real support requires QAR/FPK mutation of `master/0/00.dat` and `master/0/01.dat`, which must be implemented as a generic extension-framework capability before DMM can safely install those mods. Total War: ROME II now registers the verified Nexus domain `totalwarrome2`, live executable/pack-file diagnostics, and a research blocker because no Vortex extension exists in checked source and Steam appdetails/local Deck state do not show Workshop support. Kenshi, RimWorld, X4, Halo MCC, Spyro, FF7, Miles Morales, Prototype/2, MGSV, and Total War: ROME II still need live archive validation where supported and any future launcher-required UX contract if the game flow requires surfacing "launch through Steam" explicitly.
  Workshop-only additions: Stellaris, Dwarf Fortress, Tabletop Simulator, Nuclear Option, WARNO, Total War: PHARAOH DYNASTIES, Total War: ROME REMASTERED, Besiege, Command & Conquer Generals Zero Hour, Cultist Simulator, DiRT Rally, Plague Inc: Evolved, Stacklands, Transport Fever 2, and We Who Are About To Die now have narrow first-party Workshop extensions after Steam appdetails category `30` or live Steam Deck Workshop manifest verification. They intentionally declare no Nexus domains or archive installers until game-specific local/archive behavior is verified. Latest live deployment `f89dd7b` validated the eight newest Workshop-only extensions through `/api/extensions` and `/api/games`; each exposes five Steam Workshop actions and zero Nexus domains.
  Research-blocked additions: Final Fantasy X/X-2 HD Remaster now declares the Nexus API-verified domain `finalfantasyxx2hdremaster` plus live executable/VBF diagnostics, but all archive installs are blocked because no Vortex extension was found and representative mod layouts have not been classified yet.
  Latest manifest-backed research additions: Episode One/Episode Two and related unchecked central-manifest targets now declare verified Vortex central-manifest Nexus domains and live Steam Deck diagnostics. Their archive installers are intentionally blocked until the actual Vortex extension package/source and representative archives are inspected. Ghost Recon Breakpoint, Half-Life 2, Metro Exodus, Citizen Sleeper, Disco Elysium, Hollow Knight, Megabonk, Mewgenics, and The Binding of Isaac: Rebirth have moved out of this bucket into source-backed extension packages with varying live-validation gaps. Live `/api/extensions` and `/api/games` validation passed after package deployment; Lossless Scaling is now hidden from the user game list.
  Latest local coverage expansion: Potion Craft, Dave the Diver, Mr. Prepper, and Blasphemous moved from narrow Nexus research blockers to source-backed Unity/BepInEx installer coverage through the shared Vortex `modtype-bepinex` boundary. The framework now supports extension-declared native Linux launch tools for BepInEx runtimes and preserves executable file modes for `run_bepinex.sh` through generic install-plan metadata. Quake 4 moved to narrow idTech4 installer coverage for source-verified `q4base` replacement/package layouts, while full `+set fs_game <folder>` archives remain blocked until DMM has a dynamic per-mod launch-option API. Rome: Total War and Alexander moved to narrow source-backed data-folder replacement installer coverage; launcher/full-conversion flows remain blocked until the launch semantics are verified. Half-Life, Spelunky, and STEINS;GATE moved to narrow source-backed replacement installer coverage: Half-Life supports verified `valve` content and loose BSP map archives, Spelunky supports verified `Data` replacement archives, and STEINS;GATE supports verified `USRDIR` replacement archives. Bastion moved to platform-aware `Content/Game` config replacement support for native Linux and Windows/Proton paths, while executable patches remain blocked. Mirror's Edge moved to source-backed `TdGame/CookedPC` package replacement support, while the user-Documents mod-menu flow remains blocked pending a target-root decision. Remaining research-blocked entries are Brawlhalla, Command & Conquer Generals, Persona 5 Royal, Riders Republic, The Division 2, The King is Watching, Total War: ROME II, Final Fantasy X/X-2 HD Remaster, Prototype/2, and MGSV. These retain explicit browse/capture/diagnostic or blocked shapes until Vortex source, official handler behavior, reusable framework APIs, or representative archive behavior is verified. Package `b37289e` adds metadata-only extensions for (the) Gnorp Apologue, Braid, FEZ, Godhood, Heavy Bullets, Hotline Miami, Mark of the Ninja, McPixel, Nuclear Throne, PlanetSide 2, Shieldwall, Sleeping Dogs, SNØ: Ultimate Freeriding, STAR WARS: Rogue Squadron 3D, and Thomas Was Alone from verified ModDB/GameBanana/official/Steam pages without pretending DMM can automate those stores or install those archives yet. Live `/api/games` validation after deploy reduced the unsupported visible game count to 0 while keeping metadata-only games out of Manage Ready. Live `testing/live_extension_targets_check.sh` reported `games_checked=84 extensions_checked=84`, and live provider resolver validation passed for direct, GitHub Releases, Thunderstore, Modrinth, and GameBanana with mod.io/CurseForge correctly gated by credentials.
△ 95% More upstream providers: ModDB, Thunderstore, GitHub Releases, Modrinth, GameBanana, direct URL, local archive.
  Current implementation note: direct archive URL, Thunderstore package URL, GitHub Release asset/page URL, Modrinth project/version/CDN URL, GameBanana mod/tool/sound/spray page URL, mod.io mod page/API URL, CurseForge mod page/API URL, and local archive upload are implemented and reuse the captured-install/download-or-upload/staging/profile pipeline instead of adding provider-specific install paths. Modrinth uses the verified public REST API and requires no key for public project/version imports. GameBanana uses the verified Core/Item/Data API and requires no key for supported submission page imports; bare `/dl` and `/mmdl` links remain selected-game direct archive imports because the documented File API does not expose reliable parent mod/game metadata. GitHub Release imports now persist reversible owner/repo plus encoded release-tag/asset source IDs, so GitHub can check latest release assets through the same source-specific update path without storing raw token-bearing URLs. mod.io and CurseForge are key-gated through config-backed provider settings; phone/tablet Settings > Sources can save those keys and updates provider readiness without echoing secrets. Local archive upload is game-scoped on phone/tablet, stores the archive under the DMM cache with `local` source identity, and installs through the same installer-choice/profile flow as captured URLs. The backend exposes `GET /api/catalogs` as the provider capability read model, distinguishing ready sources from credential-gated/deferred sources. Phone/tablet Settings now has a Sources screen that shows source tags, readiness, credentials requirements, and active capabilities. Phone/tablet mod lists can filter by source and sort by source; Decky mod search/sort is source-aware. Installed-mod update checks now use optional catalog update capabilities, expose safe `source_url` values, and support Nexus plus GitHub Releases, Modrinth, Thunderstore, GameBanana, mod.io, and CurseForge where their resolver metadata and credentials are available. Nexus remains the only browse/search provider; Thunderstore, GitHub, Modrinth, GameBanana, mod.io, and CurseForge are URL-import/download only; direct archive import requires a selected game. ModDB is deferred until a supported official automated API/client path is verified. Steam Workshop remains separate platform content with installed-management only, not a remote browse/search source.
  Remaining: live-test Modrinth, GameBanana, mod.io, and CurseForge with safe archives/API keys where required, keep ModDB deferred unless a compliant non-scraper automated source is verified, add source filtering/sorting to additional advanced/history/update surfaces where useful, and live-test more non-Nexus provider archives against extension-owned installer rules.

△ 70% Local archive import must be hidden behind a Downloads-scoped browser accordion.
  Required behavior: local archive import should not dump a filesystem browser into the primary Add Mod screen. Decky and phone/tablet should show a collapsed entry such as `Import Mod Archive`; opening it starts in the Deck user's Downloads/DMM Intake roots and exposes Up Directory, Enter Path, Refresh, folder rows, and importable archive rows. The backend must reject browsing/importing outside approved archive roots, including symlink escapes and parent traversal. Local archive import remains game-scoped, install-to-profile aware, and routed through the same captured-install/profile/FOMOD pipeline as other sources.
  Remaining: live Decky controller validation and phone/tablet visual validation.

STOP / Save For Tomorrow Unless User Explicitly Resumes

□ Dependency install helpers and privilege/security model.
□ Vortex/manual adoption wizard or cleanup flow.
□ Auth/pairing/HTTPS/Tailscale-safe access.
□ External/community extension ecosystem and security model.
□ Non-Steam games, movable storage/cache settings, systemd service mode, self-update/release signing.
  Post-MVP storage scope: allow users to move DMM-owned downloads, cached archives, staging, backups, and possibly logs/db to a configurable location such as a Steam Deck SD card. This must include free-space checks, mount/path validation, safe migration, rollback if migration fails, and profile/deployment verification so existing symlinks/manifests do not point at stale storage.
